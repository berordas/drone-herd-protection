# CLAUDE.md — Proyecto escolta (drones / lobos / vacas) · AI Lab, Comillas

Contrato de trabajo para este repo. El razonamiento completo está en `DISEÑO.md`;
esto es lo que no debe perderse entre sesiones ni tras un /compact. Trabajamos en español.

## Disciplina (siempre)
- **UNA cosa cada vez.** Implementa solo lo que pide el paso/prompt actual; no te adelantes.
- El **usuario lleva el diseño**; tú tecleas y verificas. Ante una bifurcación de diseño, **PREGUNTA** — no decidas por tu cuenta.
- Ciclo de cada paso: implementar → correr la **verja de regresión** → si todo verde, commit. **Nunca commitees en rojo.**
- Antes de dar un paso por bueno: mira el render / los checks.

## Verja de regresión (córrela antes de cada commit)
`face_check.py` (12/12) · `battery_check.py` (relevo REALISTA: 4/2/2 + sin-teletransporte + cobertura continua + stranded bajo estrés + **sin-parálisis v3.0**: anunciado comandable hasta el hand-off a blanco móvil) · `escort_check.py` (terminal+disparador+guiado+disuasión) · `drone_check.py` · `reactive_check.py` · `wolf_controller_check.py` · `rl_env_check.py` (andamiaje RL, 10 tests: layout/determinismo/cap/presa/**canal de recompensa**/física intacta/equivalencia env↔evaluador/**shaping por potencial**/**residual δ=0 ≡ scriptado bit a bit**/**drones-MARL: obs por puesto + suelo δ=0 ≡ barrera bit a bit + máscara + canal global+local**)
Si algo se pone **rojo**: PARA y avisa. Nunca bajes la exigencia de un test para que pase sin decirlo.
Se corre DENTRO del contenedor del proyecto (`docker/`): el python3 del host NO tiene numpy (ver flujo DGX en DISEÑO.md §7).

## Invariantes CONGELADOS — no los toques sin permiso explícito
- Modelo vaca/lobo **"dar la cara, no apiñamiento"**: cono frontal, flanqueo, presa común fijada en t=0 (**v2.9→v3.0 — CEBO DISEÑADO Y COMPLETO, etapa 1/2, NO emergente: con spawn grouped de 2 subgrupos hay presa POR SECTOR — el 1º (CEBO) la común de siempre `pack_prey`, el 2º (ASALTO) la MÁS LIBRE `pack_prey2` = máx. distancia al dron ACTIVE más cercano, ternero-primero, capacidad del sector para adultas, excluye la del 1º si hay alternativa del mismo tipo; **v3.0 añade (gateado por `wolf_decoy_size`, solo CONFIG_V2 — con None todo es v2.9): reparto de masa FIJO** (`wolf_decoy_size=1` capado a n−2 → el asalto conserva cuórum ≥2; fin del k~unif; elegido midiendo 1-vs-2) **y TIMING PROGRAMADO del cebo** (`_decoy_prowl`: el cebo MERODEA fuera del alcance —tangencial + evasión del ACTIVE más cercano: huida radial PURA a <`decoy_hold_dist`=130 (v3.1: r_detect+30 de buffer; a 100 justo el cebo caía dentro de r_detect al asentar el merodeo y el investigador lo cazaba —15 vs 4 m/s— confirmándolo con el asalto a 238-440 m; con 130 queda 13/15 acompasado, residual 2/15 benigno: nace dentro del sobre de patrulla y aun cazado ANCLA la barrera), banda mixta 20 m, sin congelarse, sin RNG— y se LANZA cuando el centroide del asalto está a ≤`assault_trigger_dist`=150 de su presa; latch `wolf_decoy_released` en el World, patrón pack_prey; **v3.3 (congelada en v3.4): ROLES INVERTIDOS — la causa raíz del ancla robada era ancla = PRIMER confirmado y el reflejo confirmaba al GRUESO. Ahora el CEBO se deja confirmar PRIMERO (tras el disparo SE MUESTRA cargando y hace el ALA ROTA: retrocede arrastrando al investigador — busca anclar la barrera) y el ASALTO espera OSCURO por anillos (STAGE r_detect+50 → CREEP r_detect+25 → DEEP r_confirm+25, protegidos por la investigación-ÚNICA del reflejo; sustituye al ARCO OSCURO v3.2) hasta ESTACIONARSE (`_assault_staged`: pegado al sobre Y a ≤ trigger+`DECOY_SHOW_LEAD`=60 de su presa); ataca al saltar ESCOLTA o si le cazan (`_group_hunted`: cierre >8 m/s alineado cos>0.95 a ≤200). Medido (78 eps de 2 frentes): ancla=sector-cebo 13%→60% (techo ~73%: en el 27% el asalto NACE a 108-130 m de un dron y el barrido lo caza — spawn, irreducible del lado del lobo) · asalto ≤150 de su presa al saltar ESCOLTA 42-45% (frontera ancla↔profundidad ~1:1 medida en 8 configs; vías refutadas CON MEDIDA en wolf_controllers.py: show-al-dron = carrusel · estacionamiento-con-alineación = interbloqueo · CREEP+5/DEEP+10 = primer-contacto/roba-ancla · detector solo-cierre = falsos positivos)**); ambas presas/defensoras ENCARAN —pin—; táctica por sector con la MISMA maquinaria —cono/flanqueo/rodeo/envolvente—, repulsión y bordeo globales; con 1 grupo `pack_prey2=-1` y TODO es bit a bit v2.8**); **MATANZA EXCEDENTE** (tras matar O refugiarse la presa, el paquete **re-fija la res viva no-a-salvo MÁS CERCANA** y SIGUE hasta **agotar** objetivos — todas muertas o a salvo —; al agotar **coastea a parada**, sin orbitar; `n_refix` cuenta SOLO re-fijaciones por refugio); terneros + defensoras, spawn por sector, rodeo. Verificado en `face_check`.
- **ATAQUE ENVOLVENTE (`wolf_envelop_gain`, `_envelop_slots`):** el paquete reparte sus rumbos en ángulos **EQUIESPACIADOS** alrededor de la presa fijada (N→2π/N; 4→~N/E/S/O, 3→~120°), anclado al ángulo medio actual. Sin esto, contra una adulta **CLAVADA** (parada) se apiñan en el cono frontal y "dar la cara" los mantiene a TODOS a `r_face_safe` → la presa era **invulnerable** (bug). Es código de lobo COMPARTIDO; face_check 12/12 (en combate la presa se mueve y ya flanqueaba).
- **Escala biológica ABSOLUTA** (en metros, NO fracción de `min(W,H)`): `HERD_SPREAD=40`, `HERD_SEPARATION=22`, `wolf_spawn_dispersion=5`, `r_notice=20`, `r_face_safe=6`, `capture_radius=3` (+ derivados).
- **Campo: default de World 300×300 m; el MUNDO MEDIDO (CONFIG_V2, v3.0) es 500×500.** El LAYOUT escala con `min(W,H)` (establo 0.12·min=60 m a 500, central 25, refuge_margin 6, `max_episode_steps` derivado 23570); la escala biológica ABSOLUTA NO escala (r_detect/r_confirm/DETER/velocidades). v3.0 además fija `cow_spawn=(150,350)` en CONFIG_V2 → COLCHÓN ≥100 m del área de pasto a todos los bordes: un lobo naciente (perímetro) queda a >r_detect de toda vaca y de todo dron — **0/120 confirmable o detectado al nacer** (margen real de aproximación). Los checks (face/battery/escort/drone) corren con defaults 300 → bit a bit.
- **Detección/confirmación:** `r_detect=100` (hay algo: CUALQUIER cuerpo, lobo o corzo), `r_confirm=40` (ORÁCULO del tipo, verdad-terreno; placeholder determinista hasta YOLO). Fases `VIGILANCIA → SOSPECHA → ESCOLTA`. A `r_confirm`: **lobo → ESCOLTA**; **corzo → el dron DESCARTA y se libera, NO escolta**.
- **CORZOS (3c, cuerpos NO-amenaza, infra del mundo, gateada por `escort_enabled`+`corzos_max>0`):** entidad nueva (`corzos (N,2)`, `corzo_speed=4.0`); **DEAMBULAN + HUYEN** (repulsión con falloff, `CORZO_FLEE_RADIUS=30`, `CORZO_FLEE_GAIN=3`) de lobos y drones ACTIVE; **NO cazan, NO van al rebaño, NO atacan**; las vacas NO los encaran (no disparan `r_notice`). **Salen AGRUPADOS** (un centroide en la BANDA de detección del rebaño `[cow_spread+r_notice, r_detect]` → disparan SOSPECHA la mayoría de las veces, fuera del rebaño; cúmulo `CORZO_GROUP_DISPERSION=6`) y **se mantienen JUNTOS** (cohesión suave `CORZO_COHESION=0.05` al centroide + separación `CORZO_SEPARATION=4`). Son **CONTACTOS** detectables → disparan el reflejo IGUAL que un lobo; el investigador **VUELA hacia el contacto** (tipo DESCONOCIDO de lejos) y solo a `r_confirm` el **ORÁCULO** revela el tipo: **lobo → ESCOLTA** (no vuelve); **corzo O JABALÍ → DESCARTA** (`corzo_dismissed`, no se reinvestiga), el dron **VUELVE A SU PUESTO** (`drone_home`) y la **fase vuelve a VIGILANCIA** si no queda contacto investigándose (NO se queda pillada en SOSPECHA). **3 tipos de episodio** (~1/3 cada uno, sembrado: solo-lobos / solo-distracción / mixto) vía `corzo_episode_probs`; en **solo-distracción** ESCOLTA jamás → rebaño pasta, **severidad 0**. **La distracción es CORZO 🦌 o JABALÍ 🐗 (~50/50, `distraction_species_prob=0.5`), MISMO comportamiento (mismo array `corzos`, misma dinámica); la especie se elige con un SUBSTREAM RNG SEPARADO (`_distraction_rng = default_rng(seed+offset)`) → NO consume del stream principal → spawns de lobo/vaca **bit a bit** iguales (baseline comparable). El oráculo solo gana un tipo más; el dron descarta igual (no es lobo).** **OFF por defecto** (`corzos_max=0` → cero draws RNG → mundo actual **bit a bit**; el reset de fase no afecta la dinámica —solo ESCOLTA importa—; face_check/escort_check sin corzos los activan explícitamente). **`main.py` corre el mundo v2 CON corzos** (`corzos_max=3`, imprime el tipo + nº de corzos; `python main.py --escenario solo-lobos|solo-corzos|mixto` fuerza el tipo, sin forzar = sorteo; render = solo la **ventana relevante** a ritmo natural —no comprime el timeout). El POSICIONAMIENTO/descarte de drones lo aprovechará el COORDINADOR (post-v2); el clasificador imperfecto = fase YOLO.
- **Guiado al refugio (paso 2) + NO-HOLONÓMICO:** en ESCOLTA la vaca corre HACIA DONDE MIRA (huir y dar la cara EXCLUYENTES): **HUIR** (sin lobo en `r_notice`) → heading al establo + avanza de frente a `cow_speed` (velocidad SIEMPRE a lo largo del heading); **ENCARAR/PIN** (lobo en `r_notice`) → gira a encararlo y se PARA → los lobos CLAVAN a la presa (pin-and-flank). **SOLO la PRESA fijada** (y su defensora si es ternero) se para; las no-fijadas siguen HUYENDO aunque tengan lobos cerca. **Ternero a-salvo ⟺ ternero DENTRO** (no cuando lo está su madre; sigue migrando hasta entrar él). **MADRE NO ABANDONA al ternero (HUIR):** el ternero huye a su rapidez propia `calf_speed=0.8` (< `cow_speed`; la cría es más lenta) y una DEFENSORA que HUYE no lo ADELANTA → su avance se capa a `calf_speed` (la más lenta de la pareja) → migran JUNTAS a ese ritmo (la pareja es más LENTA = más vulnerable; trabajo para los drones). Solo mientras el ternero siga en juego; si ENCARA, avanza 0 (el pin manda). En pastoreo el ternero sigue capado a `cow_speed` (intacto). Es INFRAESTRUCTURA del mundo (`escort_enabled`), no el coordinador. **Pastoreo/combate sigue HOLONÓMICO** (`escort_enabled=False` = adversario PURO; face_check intacto).
- **EVITACIÓN al HUIR (ESCOLTA, solo no-fijadas):** el rumbo objetivo de HUIR mezcla `W_REFUGIO·(→establo) + W_EVITAR·Σ(alejándose de lobos en `COW_AVOID_RADIUS=30`)·falloff` → la vaca **RODEA** a los lobos en su camino (no atraviesa la pelea) y sigue llegando (el establo DOMINA el neto; la evitación es LOCAL). Sigue NO-HOLONÓMICA (avanza de frente, gira a `turn_rate`). La PRESA fijada NO esquiva (sigue ENCARANDO parada). `W_REFUGIO=1.0`, `W_EVITAR=1.3`.
- **SUSTO DE DOS RADIOS del dron (`_apply_deterrence`, infraestructura, gateada por `escort_enabled`; v2.7 — extiende el susto por movimiento de v2.4):** el miedo del lobo depende de **si el dron SE LE ECHA ENCIMA** (los depredadores se **habitúan** a disuasores estáticos A DISTANCIA), pero un lobo **NO CRUZA** un obstáculo que tiene **ENCIMA**. Fiel a los datos de fuga (FID ~106 m ante amenaza que se acerca, escalado al campo 300×300 — un radio real haría a los drones invencibles). **DOS radios anidados (mover > quieto, SIEMPRE):** **(a) EXPULSIÓN PLENA (radio GRANDE `DETER_RADIUS=20`, v2.4 INTACTA):** dron **ACTIVE ACERCÁNDOSE** (aproximación `> SCARE_APPROACH_MIN=1.0` m/s) → el lobo **HUYE** RADIAL del dron acercándose **MÁS CERCANO** (desempate por índice), `clip(wolf_speed·(1−d/R), SCARE_SPEED_MIN=0.8, wolf_speed)`; la huida **SUSTITUYE** a la caza y **NO mata mientras huye** (`_wolf_scared`, excluido de `_process_predation`). **(b) PARED BLANDA (radio PEQUEÑO `STATIC_DETER_RADIUS=10` = la mitad, v2.7 NUEVO):** un dron **ACTIVE** (aunque **QUIETO** o alejándose) a `≤ STATIC_DETER_RADIUS` y lobo NO expulsado → **NO CRUZA** hacia el dron: se le **quita la componente de velocidad que apunta al dron** (desliza/desvía) + un **empuje radial saliente de frenado** (`STATIC_DETER_GAIN=0.6`, crece al acercarse, acotado a la rapidez de caza → **FRENA y DESVÍA, no lanza**; más débil que la huida). Marca `_wolf_walled` → **NO mata a través** (excluido de `_process_predation`). Un dron estático a `> STATIC_DETER_RADIUS` **NO tiene efecto** (habituación al disuasor estático a distancia; un poste LATERAL se rodea). **Jerarquía:** si además se acerca (dentro de `DETER_RADIUS`), la **EXPULSIÓN DOMINA** (radio mayor + más fuerza) → moverse hacia el lobo es **ESTRICTAMENTE mejor** (preserva el incentivo a moverse que el MARL run01 aprendió). **EXPULSADO ≠ rendido:** al salir de ambos radios el lobo retoma la caza (sin cooldown). Marca `_drone_scaring` (drones que embisten) → el render dibuja 🔊 **solo al embestir de verdad**. Solo drones **ACTIVE** disuaden. Combate puro (`escort_enabled=False`) NO asusta ni amuralla → `_wolf_scared`/`_wolf_walled`/`_drone_scaring` todo False → **face_check bit a bit** (fingerprint de combate idéntico v2.3≡v2.4≡v2.7). **v2.7 RE-MIDE la baseline** (el dron quieto vuelve a repeler a corta → la barrera se hace PARED): Dummy **4.42/4.34 → 3.97/3.89** (Δ −0.45/−0.45), Reactive **2.74/2.82 → 2.30/2.42** (Δ −0.44/−0.40); el margen Reactive−Dummy se mantiene (−1.67/−1.47). **Efecto de diseño:** el CEBO multi-frente gana presión selectiva (una barrera atravesable no había que engañarla). **INVALIDA la baseline del MARL de drones run01** (medido sobre v2.6, barrera atravesable): run01 = histórico; el MARL se reentrenará sobre la baseline VIGENTE (v2.8). **Afinable; MEDIDA, no objetivo** (no tunear `STATIC_DETER_*`/`SCARE_*` para perseguir cifras).
- **BORDEO de ZONAS PROHIBIDAS por el lobo (refugio/central, infraestructura, gateado por `escort_enabled`):** un lobo que persigue HACIA una zona (su presa se refugió o su objetivo está al otro lado) se CLAVABA en el borde (la persecución apunta dentro, el clamp `_push_outside_circle` lo frena de frente → mismo ATASCO RADIAL que con el dron). Cerca de la frontera (`WOLF_ZONE_SKIRT_BAND=20`) una componente **TANGENCIAL** (`WOLF_ZONE_SKIRT_GAIN=3`, escala con `block`·`falloff`, sumada a `desired` antes de normalizar → solo redirige, no acelera; desempate de lado DETERMINISTA → robusto al saddle colineal) hace que el lobo **BORDEE** la zona **por FUERA** (NO entra: el clamp sigue) hasta el objetivo del otro lado. La presa refugiada se **suelta al instante** (`_prey_lost_reason`=="refuge" el mismo paso que `cow_safe` → re-fija la viva no-a-salvo más cercana, ya FUERA). Combate puro (`escort_enabled=False`) NO aplica (la presa no se refugia) → face_check bit a bit. **Zona segura = `0.12·min(W,H)`=36 m de radio** (12% del lado, 4.5% del área; central 15 m) — tamaño = decisión de diseño del usuario, NO se toca.
- **Dron:** `DRONE_MAX_SPEED=15`, `DRONE_MAX_ACCEL=4`; moverse gasta más batería que flotar.
- **RELEVO de flota REALISTA (hand-off, SIN teletransporte; infra del mundo, automática, NO del coordinador; `_step_battery`):** estados `READY → INCOMING → ACTIVE → RETURNING → CHARGING → READY` (+ `STRANDED`). **v3.0 (pieza 5 — SIN PARÁLISIS, bug corregido):** al bajar de `announce_threshold=0.20`, el ACTIVE **ANUNCIA** (`drone_relief_hold`) pero **SIGUE COMANDABLE** y cubriendo/moviéndose con la barrera hasta el hand-off (hasta v2.9 se CLAVABA en su puesto —waypoint congelado, fuera del free-mask— = agujero observado en la defensa; el flag ahora solo marca "esperando relevo": despacho + exclusión del pool de investigación); la central **DESPACHA al READY más cargado**, que **VUELA PERSIGUIENDO la posición VIVA del bajo** (`INCOMING`; blanco móvil — la línea se recoloca despacio y el INCOMING a 15 m/s lo alcanza); al estar **ENCIMA del DRON** (≤ `relay_handoff_tol=2`) → **hand-off**: relevo→`ACTIVE`, bajo→`RETURNING`→(central)`CHARGING`. **Cobertura CONTINUA** salvo **`STRANDED`** (bajo que llega a ~0 antes del relevo: se **CONGELA DONDE ESTÉ** —sin batería no se vuela—, **NO disuade** —ya no es ACTIVE—, hasta el hand-off) = el hueco real que un buen coordinador evita (no agotar la flota moviéndose de más). Verificado en `battery_check` (`test_relevo_sin_paralisis`: anunciado vuela 120 m bajo comando, hand-off a blanco móvil, cobertura 4/4, STRANDED congelado). Travel-time **EMERGE** de la dinámica de vuelo. **NO usa el RNG** (determinista → face_check bit a bit; combate corto sin relevos → intacto). La disuasión sigue keyed en `ACTIVE` (INCOMING/RETURNING/STRANDED no disuaden solos). Las **reservas parten en ROMBO** (4 vértices dentro de la central; v2.3, antes en fila recta) — determinista, no consume RNG (spawns bit a bit) → solo cambia la POSICIÓN de partida de las reservas (recogido por la re-medición v2.3; el relevo funciona igual). **v2.4: BATERÍAS INICIALES REALISTAS** — los 4 ACTIVE en vuelo arrancan con batería **ALEATORIA** en `[battery_init_min=0.25, 1]` (una flota real está a mitad de ciclo, no a tope; por encima de `announce_threshold` → sin relevos en t=0) y cada reserva EN CARGA es el **ESPEJO** de su pareja (`bat = 1 − bat_activo_i`: el que vuela lleno tiene pareja vacía, el bajo tiene pareja casi lista). Del **SUBSTREAM RNG SEPARADO** (`_battery_rng = default_rng(seed+2_000_003)`) → **NO consume del stream principal → spawns bit a bit** (face_check). **CARGA = `CHARGE_TO_FLIGHT_RATIO=1.5`× el drenaje a VUELO PLENO** (`drain_rate_active·(1+DRONE_MOVE_DRAIN)`) → tiempo de carga completa DERIVADO ≈ **160 s** (antes `charge_full=300` fijo, ahora DEPRECATED). La LÓGICA del relevo (umbrales/hand-off/estados) **no cambia**; solo el arranque energético y la tasa de recarga.
- **v2.5 CONGELADA (tag `v2.5-baseline` — SPAWN DE LOBOS EN SUBGRUPOS, Nivel A; mismo resto de física; METRO DGX):** la **FÍSICA del mundo** es la referencia que los coordinadores deben BATIR; cada cambio de física RE-MIDE y RE-CONGELA. Lineage: **v2** (tag `v2-baseline`) → **v2.1** (+ RELEVO de flota realista) → **v2.2** (+ **JABALÍ** como 2ª distracción, substream RNG separado) → **v2.3** (+ SUSTO FUERTE "campo de fuerza": Dummy 4.45/4.41 → 2.36/2.24) → **v2.4** (+ **SUSTO POR MOVIMIENTO** —solo el dron que embiste expulsa; el estático es un poste—, **baterías iniciales aleatorias + reserva espejo** y **carga 1.5× el vuelo pleno**; la disuasión estática se evapora → Dummy 2.36/2.24 → **4.41/4.34** ≈ v2.2, medida en el portátil) → **v2.4.1** (SIN cambio de física: **re-medición canónica DENTRO del contenedor de la DGX**; **el metro oficial es el contenedor `docker/`**, el portátil deja de ser referencia — sus spot-checks pueden salir rojos, esperado; Dummy 4.54/0/4.46, Reactive 2.77/0/2.80) → **v2.5** (+ **SPAWN EN SUBGRUPOS**: `wolf_spawn_mode="grouped"` — con >2 lobos, 1 ó 2 grupos al 50/50; reparto DESIGUAL k~uniforme{1..n−1} sobre los k últimos índices, k=1 = CEBO posible; 2º sector a ≥`WOLF_GROUP_MIN_ANGLE_SEP`=60° del primero, ancla proyectada al MISMO borde (cambia el eje de llegada, no cuándo); sorteo ÍNTEGRO en el substream `_wolf_group_rng` seed+3_000_003 → vacas/drones/corzos/baterías **bit a bit** vs clustered, y con 1 grupo también los lobos; **default de World = `clustered` ≡ v2.4.1 bit a bit** —face/battery/escort/drone intactos—, `CONFIG_V2` pide `grouped`; `main.py` corre grouped). **HIPÓTESIS MEDIDA (Nivel A): el multi-frente NO sube la letalidad del scriptado** — fija UNA presa común, el 2º grupo no ceba, solo llega por otro eje → ligera bajada dentro del ruido (~1 SEM). El CEBO real = fase RL posterior (Nivel B). `CONFIG_V2` fija la config; fidelidad **bit a bit** `CONFIG_V2 ≡ defaults de World + distracción ON + wolf_spawn_mode="grouped"`. `baseline.py` = **ARNÉS de evaluación**: `DummyCoordinator` sobre `range(100)` semillas × 3 tipos, guarda `baseline_v2.json`/`.csv`. **SEVERIDAD CONGELADA (N=100/tipo, DENTRO del contenedor) — v3.4 (LÍNEA RÍGIDA + ROLES INVERTIDOS DEL CEBO; 2ª pasada sin deriva):** solo-lobos **3.82**±2.41 (succ 9 / pred 83 / time 8; n_safe 2.75) · solo-distracción **0.00** (100 timeout) · mixto **3.84**±2.43 (succ 6 / pred 85 / time 9; n_safe 2.74). *(v3.2: 3.77 / 0.00 / 3.74 — el lado lobo de v3.3 apenas mueve al Dummy clavado, +0.05/+0.10; v3.1: 3.74 / 0.00 / 3.69; v3.0: 3.85 / 0.00 / 3.69; v2.9: 3.98 / 0.00 / 3.88.)* *(v2.7≡v2.8: 3.97 / 0.00 / 3.89 — el cebo diseñado apenas mueve al Dummy, ±0.01.)* *(v2.5/v2.6: 4.42 / 0.00 / 4.34 —la pared blanda baja el Dummy Δ −0.45/−0.45, sus drones clavados repelen a corta—; v2.4.1 clustered: 4.54 / 0.00 / 4.46.)* **PROTOCOLO DE COMPARACIÓN (apples-to-apples):** un coordinador nuevo se evalúa con el MISMO `baseline.py` (`evaluate(factory)`), mismas semillas y `CONFIG_V2`, **dentro del contenedor**, cambiando SOLO el coordinador; se compara la **severidad POR TIPO**. NO tocar la física sin re-medir ambas ramas.

## Arquitectura fija
- **Investigar un contacto = REFLEJO** (infraestructura, igual en todos los coordinadores): un contacto = CUALQUIER cuerpo (lobo o corzo, `_contact_bodies`); investiga el dron ACTIVE **LIBRE más CERCANO** al contacto (`_pick_investigator`; el más cercano llega antes; ocupado→siguiente más cercano libre; desempate determinista por menor índice, **sin aleatoriedad**), **VUELA hacia el contacto** (tipo desconocido de lejos), emite un mensaje al coordinador `{drone_id, contact_pos}`, **confirma a `r_confirm` por ORÁCULO**: lobo → ESCOLTA y se libera (no vuelve); corzo → DESCARTA (no se reinvestiga), **vuelve a su PUESTO** (`drone_home`) y la **fase vuelve a VIGILANCIA** si no queda contacto investigándose (no se queda en SOSPECHA).
- El **COORDINADOR** (lo que se compara) recoloca a los **demás** drones con ese mensaje. Dummy = no recoloca (baseline). Precedencia: el reflejo manda sobre el dron que investiga; el coordinador, sobre el resto.
- **ReactiveCoordinator (`coordinators.py`, primer coordinador de verdad — regla FIJA, NO aprende; la referencia CLÁSICA que el MARL deberá batir):** forma una **BARRERA de apantallado** entre la manada de lobos y las vacas más cercanas a ella (frente perpendicular al eje rebaño→manada; **v3.2 — BARRERA SIN HUECOS + AVANCE REAL (regla del usuario; el criterio de aceptación fue el GIF, no el test)**: diagnóstico previo con datos — la línea v3.1 vivía PEGADA al rebaño (front_c ≈ centroide a 0.8 m, adv ≤ 36.7 → solo "perseguía" con el ancla a <27 m, la banda de su test 12): 17.9% de pasos de ESCOLTA con la formación a <1 m/s (incapaz de expulsar), **75.4% de encuentros cercanos SILENCIOSOS** (lobo a ≤DETER de un ACTIVE sin susto ni pared) y **~4.6 cruces de línea/ep** por las franjas sin pared (spacing 32 > 2·STATIC=20). Arreglos v3.2: **(1) spacing = 2·`STATIC_DETER_RADIUS` = 20 m** — las paredes blandas de drones ADYACENTES SE TOCAN: cruzar la línea siempre mete al lobo en radio de pared (**0 cruces limpios medidos**; la línea estrecha ~60 m se RODEA por los FLANCOS — limitación ACEPTADA de la baseline ingenua, otro agujero deliberado junto al 2º frente); standoff derivado recalculado sqrt(20²−10²) ≈ 17.3; **(2) la formación PERSIGUE al ancla**: `aim = clip(d(centroide→ancla) + STATIC_DETER_RADIUS, borde_delantero+standoff, MAX_ADVANCE_FROM_HERD=50)` medido desde el CENTROIDE de collares — empuja hacia la amenaza mientras el centro quede a ≤50 m del centroide (50 = radio del rebaño ~20 + ~30 de salida, afinable); resultado medido: **45% de pasos de ESCOLTA con persecución real** (algún dron cerrando >1 m/s sobre un confirmado), silenciosos 75.4→62.5%, centro ≤ anillo verificado (0 violaciones). Sustituye al tope advance_max≈36.7 desde front_c (~39 del centroide): el anillo de 50 llega ~11 m más lejos a costa de que un lobo por la ESPALDA de la línea ya no sea confirmable por ella (en ESCOLTA no se lanzan investigaciones) — agujero DELIBERADO documentado. **DOS FRENTES confirmados → la línea ENTERA va al ancla y el 2º queda LIBRE** (mover el 2º confirmado no cambia ni un waypoint): defensa DELIBERADAMENTE explotable — el agujero que el MARL de drones deberá aprender a no tener) para que la **DISUASIÓN** (automática, de los ACTIVE) frene a los lobos antes de alcanzar al rebaño. **COORDINADO** (reparte el frente, no cada uno a por un lobo), **REACTIVO** (recalcula cada paso), **defiende a TODO el rebaño** (NO usa la presa fijada; solo ve posiciones de lobos y de vacas vivas no-a-salvo, incl. terneros). **PENETRADO** (la manada ya está entre las vacas): degrada a cubrir a los lobos más cercanos a ellas (`engage_standoff`=2·`r_face_safe`). **Sin amenaza** (VIGILANCIA/SOSPECHA, solo-corzos): **PATRULLA** en órbita REPARTIDA alrededor del rebaño (la FASE de la formación se ancla —media circular— a la posición angular ACTUAL de los drones → desde el paso 0 cada uno va a su ranura MÁS CERCANA, sin cruzar el centro; luego órbita rígida con `patrol_omega`, sin reasignaciones). Solo comanda a los **ACTIVE LIBRES** (no toca el reflejo/investigador ni los relevos de batería: les deja su waypoint); recibe el estado del mundo (`ReactiveCoordinator(world)`) y devuelve waypoints. **PERCEPCIÓN HONESTA (v2.8):** la barrera reacciona SOLO a lobos **CONFIRMADOS de equipo con MEMORIA** (`_confirmed_wolves`): un lobo queda CONFIRMADO en cuanto está a ≤`r_confirm`=40 de algún dron ACTIVE (el nivel IDENTIFICAR del marco DRI, el MISMO r_confirm del oráculo del mundo) y **se recuerda el resto del episodio** (latch/tracking, compartido por la flota; reset solo ante episodio nuevo); un contacto NUNCA confirmado **NO existe para la barrera** aunque esté a ≤`r_detect` (bulto sin clasificar — el reflejo de investigación del MUNDO se ocupa de él); el EJE se ancla al PRIMER confirmado (memoria + histéresis; con el latch el ancla es estable); PENETRADO y la cobertura lobo-a-lobo operan SOLO sobre confirmados; en ESCOLTA sin NINGÚN confirmado → PATRULLA. Modelado como oráculo determinista; la fase YOLO+tracking lo sustituirá con la MISMA interfaz. Lineage: v2.5 `wolves.mean()` = omnisciencia de POSICIÓN → v2.6 detectados a `r_detect` = seguía siendo **percepción-ORÁCULO** (sabía el TIPO de un bulto a 100 m; los corzos jamás entraban en la máscara) → no dejaba frente ciego y el cebo era imposible por ARTEFACTO (diagnóstico post-run06); v2.8 lo cierra: **el FRENTE CIEGO existe** → el cebo multi-frente es geométricamente posible. La máquina de fases del mundo NO cambia (ESCOLTA ya dispara por confirmación). Fórmulas intactas (ranuras/reparto/patrulla). **NO toca `world.py`** (física congelada) — la disuasión del mundo hace el trabajo. **Evaluado con el MISMO arnés** (`reactive_eval.py` → `baseline.evaluate`, mismas semillas/CONFIG_V2): en **v2.4 (SUSTO POR MOVIMIENTO)** la barrera **SUBE mucho** respecto a v2.3 (0.16/0.18 → ~2.8) porque los drones **clavados en el frente son POSTES** que ya no disuaden solos; pero **SIGUE batiendo al Dummy** con holgura. **v3.3/v3.4 ENCIMA de la v3.2 — TRINQUETE + LÍNEA RÍGIDA:** el aim es MONÓTONO hacia fuera por episodio (trinquete `_aim_out`, v3.3 — métrica del usuario: dist centro→vaca más próxima CRECE mientras los lobos se acercan: 58% ventanas, series 13→50-60 sostenidas; el aim v3.2 perseguía también HACIA DENTRO = paseo aleatorio) y la barrera es una **LÍNEA RÍGIDA (v3.4, regla del usuario: si un dron avanza, avanzan TODOS)**: UNA pose (centro C + eje u) con offsets FIJOS — waypoint_i = C + off_i·perp SIEMPRE, ningún objetivo individual (el CIERRE LOCAL v3.3 ELIMINADO: deformaba por definición; coste MEDIDO en 28 eps: cruces de segmento 56→149, hacia-dentro-línea-en-pie 33→92 ≈ 3.3/ep, PENETRADO 4159→13593 — la pared blanda congelada canaliza por los corredores, es muro frontal, no valla) — con **GOBERNADOR DEL MÁS REZAGADO**: paso_pose = max(0, `DRONE_MAX_SPEED`·dt − err_max), desplazamiento (traslación+rotación) ≤ λ·(|Δc|+|Δφ|·r_max) — la formación avanza al ritmo del más rezagado (equilibrio ~3.1 m/s con err ~1.2 = retraso del perfil de frenada); pose IDEMPOTENTE por paso; en la (re)formación nace en el objetivo y espera QUIETA a que los drones lleguen. RIGIDEZ MEDIDA (28 eps, FORMADA = 85% de los pasos CLEAN): err de estación p95 1.66/máx 4.71 m · espaciado real p5-p95 19.7-20.3 (nominal 20) · colinealidad p95 0.20 m. **Números VIGENTES (v3.4, metro DGX): solo-lobos 3.82→2.68±1.66 (−1.14), mixto 3.84→2.77±1.71 (−1.07), solo-corzos 0**; n_safe 4.16/4.10, success 5/7 (MEDIDA, no objetivo; sin tunear). **VEREDICTO v3.4: el cebo INVERTIDO + la línea rígida encarecen la defensa (+0.77/+0.81 vs v3.2 1.91/1.96) — POR FIN el multi-frente scriptado rinde**; agujeros DELIBERADOS: flancos de la línea estrecha + CRUCES residuales por los corredores + espalda del anillo de 50 + 2º frente libre (ahora explotado por el cebo). **TIMING DEL CEBO medido con LA MÉTRICA CORRECTA (v3.2, dist del asalto a su presa en el instante en que salta ESCOLTA):** v3.1 daba 58% ≤150 (mediana 144) — la "aceptación 13/15" de v3.1 medía otra cosa (el estado al asentarse, no el instante del disparo); anatomía de la carrera (inv@==det@ en 78/78: la investigación se lanza el MISMO paso de la detección y confirma ~15 m más adentro → pasa ⟺ el barrido detecta al asalto a ≤~165 de su presa). Arreglo v3.2 en el LOBO (`_assault_approach`): el asalto pre-disparo hace **ARCO OSCURO + PICADO** (orbita fuera del alcance del barrido —hold r_detect+30, el buffer del cebo— hasta ALINEARSE con el rumbo de SU presa y pica en recta; si ya está detectado o un investigador le cierra a >8 m/s, carga PURA) → **63% ≤150, mediana 143**; el resto es ESTRUCTURAL de la defensa honesta (13% cebo cazado al NACER a 4 vs 15 m/s — cola 300+ irreducible—, ~9% asalto barrido en su propio tránsito de nacimiento, resto detección lejana con el rebaño disperso — la patrulla escala con el spread). **4 vías refutadas CON MEDIDA** (esquive radial 58→51% · falda tangencial+sprint 58→47% · disparo adelantado gateado 56% · evasión anticipatoria del cebo neutra) — documentadas en wolf_controllers.py para no re-descubrir el valle. (Lineage Reactive: v2.5 omnisciente 2.71/2.76 → v2.6 detectados 2.74/2.82 → v2.7 pared 2.30/2.42 → v2.8 honesta 2.56/2.26 → v2.9 avance+cebo 2.46/2.44 → v3.0 cebo perfecto + defensa con perseguidor 1.00/1.14 (artefacto) → v3.1 formación coherente 2.18/2.21 → v3.2 sin huecos + avance real 1.91/1.96 → v3.4 línea rígida + cebo invertido 2.68/2.77.) **Conclusión de diseño:** con el susto por movimiento **defender bien exige MOVER los drones con intención** — y la línea rígida v3.4 lo hace con UNA pose gobernada (sin deformarse); la nota a batir del **MARL** pasa a **2.68/0/2.77** (v3.4, la línea rígida contra el cebo invertido, con sus agujeros deliberados). Verificado en `reactive_check.py` (barrera repartida / reactivo —umbral 3 m, escala con el standoff— / sin-presa-fijada / penetrado / **percepción honesta v2.8: A contacto-sin-confirmar no dispara barrera + confirmación al cruzar 40 + memoria al alejarse; B frente ciego no influye en ni un waypoint** / **standoff: peor hueco real 20.0 ≤ DETER_RADIUS** / patrulla / arranque repartido sin cruces / **severidad muestra Reactive ≤ Dummy** / reproducible) + `reactive_barrera.gif` / `reactive_patrulla.gif`. (Lineage: disuasión PARCIAL v2/v2.1/v2.2 → Dummy 4.45/4.41, Reactive 3.27→3.42; susto FUERTE estático v2.3 → Dummy 2.36/2.24, Reactive 0.16/0.18.)
- Collares/guiado y batería = **infraestructura del mundo**. Solo se compara el coordinador de drones.
- **ANDAMIAJE RL de LOBOS (`rl/` + `docker/` + `rl_env_check.py` — fontanería completa, SIN entrenar en serio aún):** cerebro ÚNICO del paquete; **PPO (SB3) + Gymnasium single-agent** (`rl/wolf_env.py: WolfPackEnv`; PettingZoo = fase de drones). **Acción** `Box(-1,1,(10,))` = velocidad por slot (5×2, desnormaliza ×`wolf_speed`), decidida cada `frame_skip=5` pasos (0.5 s) y MANTENIDA entre decisiones; **el CAP por NORMA vive en la FRONTERA del controlador** (`rl/rl_wolf_controller.py` recorta a `wolf_speed` antes de devolver — world.py CONGELADO no se toca; cumple el "recortará al aprendido" del contrato, revisable). **Recompensa:** +1 por res matada (Δ`n_depredadas`), sin castigo por tiempo/a-salvo; **+ SHAPING POR POTENCIAL (plan B ACTIVADO, decisión del usuario 2026-07-15, tras el aborto de run01):** r_shape = γ·Φ(s′)−Φ(s) con Φ = −β·(dist media del paquete a la presa designada por la regla ternero-primero, GUARDAR/RESTAURAR — el pin no debe ver el cálculo de la frontera)/diagonal del campo, Φ=0 en coasting; γ EXACTAMENTE el del PPO (0.999, entra al env como parámetro — inocuidad de Ng et al. 1999), β=`--shaping-beta` (def. 1.0); s/s′ = FRONTERAS del step del env (tras frame-skip). Componentes por separado en `info` (r_kills/r_shape + ep_kills/ep_shape al terminal) y en `train.log` (ep_kills_mean = LA señal; ep_shape_mean ≠ 0 desde el arranque). **La eval NUNCA ve el shaping** (eval_wolves/eval ligera van por el World y cuentan muertes); default del ENV = OFF (`shaping=False` ≡ run01 bit a bit; `--shaping` de train_wolves def. ON). Verificado en **rl_env_check test 8** (telescopia exacta/signo/kills intactos/off≡run01). **Obs fija (122, float32)** con padding+máscaras, marco relativo al establo, SIN corzos/batería/presa (layout con índices = docstring de `wolf_env.py`, la referencia; se lee del World directo — `get_observation()` es parcial). **`pack_prey` por REGLA FIJA** (ternero vivo no-a-salvo más cercano al centroide del paquete; si no, vaca; nada → -1/None) y **coasting DETERMINISTA** (`_targets_exhausted`) — NO los decide la red. **Episodios lobos/mixto ~50/50, NUNCA corzos**; cada `reset()` semilla FRESCA de secuencia propia (¡`World.reset(None)` repite episodio!); adversario = **ReactiveCoordinator congelado** dentro del env. **Arnés extendido RETROCOMPATIBLE:** `build_world(…, wolf_controller=None)` + `evaluate(…, wolf_controller_factory=None)` (None = bit a bit lo de siempre — verificado: HEAD y extendido dan lo MISMO en el mismo entorno). **METRO CANÓNICO (v2.4.1, decisión 2026-07-14):** la baseline congelada resultó DEPENDIENTE DEL ENTORNO (deriva FP portátil↔DGX amplificada por el caos; spawns/RNG idénticos) → **el entorno oficial de medida es el CONTENEDOR del proyecto en la DGX** (`docker/`); toda evaluación comparable (baseline, reactive_eval, lobos aprendidos) se corre DENTRO. El portátil deja de ser referencia de números: sus spot-checks contra artefactos pueden salir rojos — esperado. **`make_wolf_controller("learned")` SIGUE en NotImplementedError** (el RL inyecta `wolf_controller=` directo; el cableado del modelo entrenado = paso posterior). **Docker del proyecto (`docker/`):** imagen calcada del lab (pins exactos + libgl1), contenedor `${USER}-wolves` idle (`conectar.sh`), **GPU count:1**, repo→`/workspace`, `~/rl_data`→`/data` (checkpoints/logs FUERA del repo), `PYTHONPATH=/workspace`; **`docker compose down` al terminar la sesión** (buen vecino). `rl/train_wolves.py` = PPO MlpPolicy **[256,256]** (desde el plan C; [128,128] hasta run02 — su ablación es cross-arquitectura) + SubprocVecEnv(**fork**) + VecMonitor → checkpoints/tb/config/summary en `/data`; `--smoke` 60k/4 envs/CPU (smoke: 681 fps, 60 eps, sin NaNs; recompensa 0.0 = política aleatoria vs barrera, esperado — el canal lo prueba el test dirigido del check con 21 muertes). **EVALUADOR de lobos aprendidos:** la obs vive en **`rl/obs.py`** (`build_obs(world)`, ÚNICA fuente de verdad del layout — env y evaluador la comparten); **`rl/policy_wolf_controller.py`** = `PolicyWolfController` (modelo SB3 dentro, predict determinista, mismo frame-skip; hereda cap/presa/coasting de RLWolfController) + **`SyncedReactiveCoordinator`** (el refresco de la política va en `act()`, la FRONTERA del step — el mismo instante en que el env muestrea; `decide()` solo aplica; equivalencia env↔evaluador BIT A BIT = **rl_env_check test 7**, hermético); **`rl/eval_wolves.py`** = arnés de siempre (`evaluate`, mismas 100 semillas) vs los scriptados (2.77/2.80; MÁS muertes = mejores lobos), JSON a /data. `train_wolves`: gamma **0.999**, `--resume`, `--shaping on|off` (def. on) + `--shaping-beta`, checkpoints ~500k, `train.log` legible (ep_rew/ep_kills/ep_shape/ep_len) + **eval ligera** cada 250k (10 eps deterministas, SIN shaping); Criterios de aborto por modo en el log (shaping: kills≈0 a 3M · warm-start: eval ligera < mitad del clon ≥1,5M). **PLAN C (`rl/collect_demos.py` + `rl/bc_pretrain.py` + `--init-from`):** demos del scriptado en las FRONTERAS del env (semillas 10.000+ disjuntas del examen, acción ÷wolf_speed, **presa del CONTRATO RL impuesta** —el pin del servicio; sin esto las etiquetas dependen de la histéresis oculta del scriptado—), BC = MSE/dir ENMASCARADA (slots presentes) sobre la media de la gaussiana de la MISMA política PPO (solo π; value y log_std frescos), `--init-from` copia solo tensores de π. **ESTADO: cuna BC ≈0** (el scriptado por el mismo pipeline: puro 2.7 · +hold 2.7 · +presa contrato 2.5 = techo; el clon infraajusta la asignación discontinua del envolvente con [128,128]: |pred|~0.62 vs |exp|=1.000, train≈val alto, llega a r_face_safe pero no completa el flanqueo) → **run03 NO lanzado; decisión del usuario pendiente**.
- **MARL de DRONES (`rl/drone_*.py` + test 10 de rl_env_check — INFRA verificada + run01 COMPLETADO: EL MARL BATE LA BARRERA):** **⚠️ run01 medido sobre v2.6 (barrera atravesable) → INVALIDADO como baseline por v2.7-v3.4. run01 queda HISTÓRICO; el MARL se reentrenaría sobre v3.4, nota a batir Reactive **2.68/0/2.77** (los agujeros DELIBERADOS que el MARL deberá aprender a no tener: 2º frente libre —ahora EXPLOTADO por el cebo invertido—, flancos de la línea estrecha, cruces residuales por los corredores y espalda del anillo de 50). El residual de drones hereda la barrera v3.4 (línea rígida) automáticamente (vive en `coordinators.py`; su δ se suma al waypoint rígido — la pose idempotente por paso lo soporta); re-verificar su suelo δ=0 ≡ 2.68/0/2.77 antes de cualquier run. v3.0 además cambia la máscara comandable a `ACTIVE&~investigating` (el anunciado de relevo ya no se excluye — pieza 5; `drone_obs`/`residual_drone_coordinator` actualizados).** run01 (20M pasos-agente, 12 mundos, lr 1e-4, pacto `c215689`, /data/drones/run01): 100 semillas FINAL (20M = model.zip) **2.35±1.33 / 0.00 / 2.34±1.42** → **Δ −0.39/−0.48** vs barrera 2.74/0/2.82 (~3 SEM, consistente en ambos tipos; n_safe 4.54/4.54) · mejor ckpt 18,99M 2.41/0/2.42 (Δ −0.33/−0.40) — robusto al checkpoint; guardias (erosión >2.9 sostenida / anti-proxy ep_deter↑ sin ep_sev↓ / sin estancamiento) JAMÁS disparadas; fase 1 clavada en el suelo de la ligera 2.80 EXACTO ×4. **Diagnóstico (58 gemelos de 2 frentes):** el Δ = MOVERSE PARA DISUADIR (+17% rapidez ACTIVE, +26% lobos huyendo/paso, sev 2-frentes 3.31→2.90) — vence la HABITUACIÓN v2.4; NO aprende reparto entre frentes (6.2%→6.5%; coherente con run05: el scriptado no ceba). La mejora crecía al final (evals >18M media 2.125) → `--resume` es candidato. Siguiente = decisión del usuario. MAPPO/CTDE **sobre el SB3 pinneado, sin dependencias nuevas** (sin PettingZoo/BenchMARL): actor COMPARTIDO DESCENTRALIZADO + crítico CENTRALIZADO vía obs COMPUESTA [local 131 ‖ global 122] con `SplitMlpExtractor` (π ve solo la local; V la global privilegiada = `rl.obs.build_obs` REUTILIZADA — verdad-terreno, CTDE). **AGENTE = PUESTO de barrera** (asiento k = k-ésimo dron EN ESTACIÓN, ACTIVE∪STRANDED por índice — los relevos cambian el dron, no el puesto). **Obs local por puesto** (`rl/drone_obs.py`, layout con índices en su docstring): EGO (+ `commandable` + WAYPOINT BASE de la barrera como pista — su histéresis/patrulla son estado oculto) · lobos SOLO DETECTADOS (criterio DRI compartido, `detected_mask`) · vacas/terneros/roster · SIN batería, SIN corzos. **Controlador residual** (`rl/residual_drone_coordinator.py`): la barrera VIVE dentro (hereda `coordinators.py` — desde v2.8, la honesta); δ aditiva al waypoint (escala def. `DETER_RADIUS`, flag) SOLO a drones COMANDABLES — **máscara LOAD-BEARING** `ACTIVE&~investigating&~relief_hold` (el mundo solo protege investigador+relevo; un δ a un RETURNING rompería la carga); `model=None` sin δ ⇒ delegación pura = **SUELO 2.74/0/2.82 por construcción** (verificado: 100 semillas Δ +0.00 exacto + test 10b bit a bit). **Env** (`rl/drone_env.py`): `DroneTeamEnv` conjunto (SubprocVecEnv fork tal cual) + `TeamUnstackVecEnv` (M mundos → 4M streams = parameter sharing); episodios de los TRES tipos; **recompensa global (−1/muerte compartida) + local (disuasión atribuida por `deter_credit`, la regla del mundo recomputada determinista) POR SEPARADO** en info/log (ep_sev/ep_deter — vigilancia ANTI-PROXY: la vara es la severidad del arnés). **Entrenador** (`rl/train_drones.py`): receta residual de lobos (HYPER/[256,256], init δ≡0 assert, log_std −2, 2 fases solo-crítico→PPO), LightEval por SEVERIDAD (MENOS=mejor; suelo ligera ~2.90), num_timesteps = pasos-AGENTE (mundo ≈ /4). **Evaluador** (`rl/drone_eval.py`): `evaluate(coordinator_factory=...)`, misma vara que Dummy/Reactive; `--floor` = cableado. El run serio (con su pacto de guardias) = decisión del usuario.
- **Controlador de LOBOS ENCHUFABLE (`wolf_controllers.py`, refactor — prepara la fase RL):** los lobos tienen un "cerebro" intercambiable como los drones. `WolfController.decide(world) → (v_target (nw,2), coasting)` devuelve la **VELOCIDAD deseada** por lobo (la INTENCIÓN = acción del futuro RL); el default `ScriptedWolfController` es el comportamiento v2.4 **BIT A BIT** (el scriptado movido tal cual). **FRONTERA POLÍTICA/FÍSICA (contrato del RL): POLÍTICA** (→ controlador): fijación/re-fijación de presa, standoff, cono/flanqueo, rodeo, envolvente, repulsión, bordeo de zonas, coasting. **FÍSICA** (→ mundo, innegociable): cap `wolf_speed` + inercia/integración; el **SUSTO** (`_apply_deterrence`, IMPONE la huida sobre la intención —un lobo huyendo no mata—); captura/muerte; percepción; dinámica de vacas/terneros/corzos; clamps de zona. **`pack_prey` VIVE en el World** (contrato compartido: la LEE el 'pin' de la vaca —una vaca solo encara si es la presa fijada—, la instrumentación y el render; el controlador la ESCRIBE). El mundo NO recorta la salida del scriptado (cap implícito, bit a bit; recortará al aprendido). `_update_wolves` = orquestador fino (`decide → susto → inercia/integración`). Selección: `World(wolf_policy="scripted"|"learned")`, `main.py --lobos scripted` (`learned` = hueco RL, `NotImplementedError`). Verificado: fingerprint bit a bit vs v2.4 (`4425c866…`) + `wolf_controller_check.py` (susto innegociable + spot-check baseline). SIN re-congelar.

## Orden de construcción
3a movimiento de drones ✓ → 3b detectar→confirmar ✓ → **paso 2 guiado al refugio ✓** → **disuasión del dron ✓** → **matanza excedente ✓** → **fix pin + envolvente ✓** → **evitación al huir ✓** → **el más cercano investiga ✓** → **afinar disuasión (radio corto + bordeo) ✓** → **madre no abandona al ternero ✓** → **lobos no se pillan en la zona segura ✓** → **3c corzos ✓** → **congelar v2 ✓** → **coordinador reactivo: barrera de apantallado ✓** (sev por tipo 3.27 / 0 / 3.40 vs Dummy 4.45 / 0 / 4.41) → **relevo de flota REALISTA ✓ (v2.1: hand-off, sin teletransporte)** → **render: emojis + barra de batería + `--coordinador` ✓** → **jabalí como 2ª distracción + emojis más pequeños ✓ (v2.2: RE-CONGELADO tag `v2.2-baseline`, mismos números)** → **retoques visuales (emojis aún más pequeños, sin leyenda de entidades, 🔊 al disuadir) + fix arranque del reactivo (patrulla anclada, sin cruces) ✓ (Reactive 3.36 / 0 / 3.42; Dummy y física INTACTOS, NO re-congela)** → **SUSTO FUERTE + rombo de carga ✓ (v2.3: la disuasión pasa de PARCIAL a fuerte —un dron ACTIVE EXPULSA al lobo, que no mata mientras huye—; RE-CONGELADO tag `v2.3-baseline`; Dummy 2.36 / 0 / 2.24, Reactive 0.16 / 0 / 0.18)** → **SUSTO POR MOVIMIENTO + baterías espejo + carga 1.5× ✓ (v2.4: solo el dron que EMBISTE expulsa —el estático es un poste—; baterías iniciales aleatorias + reserva espejo (substream separado); carga = 1.5× el vuelo pleno ≈160 s; RE-CONGELADO tag `v2.4-baseline`; la disuasión estática se evapora → Dummy 4.41 / 0 / 4.34 ≈ v2.2, Reactive 0.16/0.18 → 2.80 / 0 / 2.78 —sube, pero sigue batiendo al Dummy—)** → **andamiaje RL de lobos ✓ (contenedor `docker/` + `rl/`: WolfPackEnv Gymnasium + RLWolfController + train_wolves PPO + `rl_env_check.py`; smoke 60k OK, física intacta)** → **v2.4.1-baseline ✓ (mismo mundo, METRO DGX: el contenedor pasa a ser el entorno canónico de medida; Dummy 4.54/0/4.46, Reactive 2.77/0/2.80)** → **run01 (rala pura) ABORTADO a 2,48M ✓ (criterio pactado: 0 señal)** → **plan B: SHAPING POR POTENCIAL ✓ + run02 de cero COMPLETADO ✓ (10M; kills 0→0.35-0.40 en buffer; eval 100 semillas 0.57/0.60 vs 2.77/2.80 = ABLACIÓN desde-cero)** → **plan C (clonar al scriptado + fine-tune): collect_demos + bc_pretrain + --init-from construidos ✓, pero CUNA BC ≈0 con [128,128] Y TAMBIÉN con [256,256] (opción A del usuario; val casi no mejora) → causa raíz por capas, todas MEDIDAS: pipeline ✓ (scriptado por el mismo camino 2.5–2.7) → presa del contrato ✓ → capacidad ✗ ([256,256] no movió la val) → ALIASING de la etiqueta (31% de flips a 0,5 s) → **A′: etiqueta = MEDIA de la ventana (dataset v3, `collect_demos --label mean`); su TECHO en vivo (oráculo deepcopy, /data/wolves/diag/) = 2.60 — etiqueta EXONERADA; pero el clon sigue 0.2/10: ajusta el VIAJE (cos 0.83; cruza la barrera, llega a la presa) y NO la MANIOBRA de flanqueo (|exp|<0.3: cos 0.26, 37% opuesta) → el cuello es la RED/representación sobre la obs congelada** → **DECISIÓN (C): run03 = PPO desde el clon v3 CON shaping (β=1 — la cuna 0.2 no da recompensa densa; el shaping es invariante (test 8) = red de seguridad del warm-start; run02 (de cero) vs run03 (desde clon) aísla el valor de la cuna); lr 1e-4, 10M, aborto adaptado: a ~3M ni kills ni evals ligeras superan con claridad ~0.2 → parar** → **run03 COMPLETADO ✓ (criterio no saltó: ligera 0.70 a 3,25M, picos 0.80; 100 semillas: FINAL 0.65/0.72 · mejor 4M 0.66/0.71 — LA CUNA APORTA vs run02 0.57/0.60, único cambio el init; el scriptado 2.77/2.80 y el techo 2.60 siguen lejos: la MANIOBRA de flanqueo es el muro — el Φ de distancia premia acercarse, no flanquear; siguiente movimiento = decisión del usuario)** → **run04: política RESIDUAL sobre el scriptado (RPL, Silver et al. 2018 — BIBLIOGRAFIA.md NUEVO): el script VIVE dentro del controlador (`rl/residual_wolf_controller.py` — SU presa/histéresis/coasting; nada del contrato RL) y la red aprende solo δ aditiva (`v_final = clip_norma(v_script+δ)`, δ cada 5 pasos, obs 132 = 122 + acción del script normalizada como pista del estado oculto; en coasting δ no se aplica); env `residual=True` (acción δ×`residual_scale`, def. wolf_speed); entrenador en 2 fases (init δ≡0 EXACTO verificado + log_std −2 → FASE 1 solo-crítico 1M congelando π → FASE 2 PPO lr 1e-4); recompensa RALA PURA (el script ya da ~2.4-2.6 kills/ep de señal); SUELO verificado: test 9 (δ=0 ≡ scriptado BIT A BIT, episodio completo) + `eval_wolves --floor` (100 semillas); GUARDIA DEL SUELO en el log: eval ligera < 2.3 sostenida ≥1M en fase 2 → parar (PPO erosionando al script). La cifra final que importa: Δ SOBRE el scriptado (2.77/2.80 = suelo Y vara)** → **run04 COMPLETADO ✓ (andamiaje impecable: fase 1 clavada en 2.90×4 idénticas, guardia jamás amenazada; 100 semillas: mejor ckpt 1,5M 2.82/2.81 = Δ +0.05/+0.01 DENTRO del ruido, final 10M 2.61/2.66 = Δ −0.16/−0.14 erosión leve — los picos 3.1-3.2 de la ligera eran ruido de n=10). LECTURA: contra esta barrera el SCRIPTADO ya está ≈ en el techo explotable; 4 runs cierran la fase de lobos aprendidos (rala 0 → shaping 0.57/0.60 → cuna 0.65/0.72 → residual ≈ suelo); el scriptado queda como lobo canónico — siguiente movimiento = decisión del usuario** → **v2.5: SPAWN EN SUBGRUPOS (Nivel A) ✓ RE-CONGELADO tag `v2.5-baseline` (`wolf_spawn_mode="grouped"` en CONFIG_V2/main.py, default World = clustered ≡ v2.4.1 bit a bit; substream seed+3_000_003; hipótesis multi-frente MEDIDA: NO sube la letalidad del scriptado — Dummy 4.54/4.46→4.42/4.34, Reactive 2.77/2.80→2.71/2.76, ~1 SEM: fija UNA presa común, el 2º grupo no ceba; el cebo real = RL Nivel B posterior)** → **v2.6: PERCEPCIÓN REALISTA de la barrera ✓ RE-CONGELADO tag `v2.6-baseline` (la barrera del Reactive se ancla al lobo DETECTADO —criterio `r_detect`/ACTIVE del mundo, ancla = primer detectado con histéresis, PENETRADO solo sobre detectados, sin detectados → patrulla— en vez de a `wolves.mean()` omnisciente; SOLO `coordinators.py`, física ≡ v2.5 → Dummy BIT A BIT 4.42/0/4.34; Reactive re-medido 2.71/2.76→2.74/2.82 —+0.03/+0.06, dentro del ruido: contra el scriptado convergente casi todo lobo amenazante está detectado—; lo que cambia DE VERDAD: el frente NO VISTO ya no influye → el cebo del Nivel B es físicamente real; `test_percepcion` nuevo en reactive_check, tests 1-9 sin cambios de aserción)** → **run05 Nivel B CERRADO por la guardia de ESTANCAMIENTO (pactada) a 4.2M: el CEBO NO EMERGE con recompensa de equipo PURA. Setup: residual de run04 tal cual sobre v2.6 grouped, +1/muerte compartida, shaping OFF, SIN pista de cebo, δ autoridad plena, 2 fases (fase 1 clavada en 2.90 detalle idéntico ×4), 12 envs, lr 1e-4; suelo verificado 2.74/2.83. Desenlace: 12 ligeras de fase 2 en 2.50-3.10 ≈ suelo sin tendencia → PARADO; 100 semillas: ckpt 4M 2.67/2.71 (Δ −0.07/−0.11) · ckpt 3.5M 2.80/2.71 (Δ +0.06/−0.11) ≈ suelo. **`rl/cebo_diag.py` (la métrica que importa): killer-no-detectado 0.0% y cebo puro 0.0% en AMBOS checkpoints = la MISMA nula del scriptado — ni UNA muerte desde el frente no visto**; δ cambió comportamiento (detalles ≠ suelo) pero hacia ruido/erosión (sev en episodios de 2 grupos 3.33→3.09/3.24). Lectura estructural: el cebo = desviación coordinada y temporalmente extendida con VALLE de recompensa en medio (un cebo a medias quita quórum antes de dar nada) — la exploración gaussiana por-paso no lo encuentra. Candidatos si se insiste (decisión del usuario): pista al cebo (anti-proxy) / control explícito de formación / co-evolución** → **INFRA MARL de DRONES ✓ (MAPPO/CTDE residual sobre la barrera, SB3 pinneado sin deps nuevas; agente=PUESTO; obs local con lobos solo DETECTADOS + crítico centralizado privilegiado; recompensa global+local por separado; SUELO verificado δ=0 → 2.74/0/2.82 con Δ +0.00 exacto en 100 semillas; test 10 + verja 7/7 + smoke 2 fases; SIN run serio)** → **MARL run01 ✓ COMPLETADO — BATE LA BARRERA (el primer coordinador que baja del suelo):** pacto fijado antes del run (`c215689`: ligera 5 lobos+5 mixto con suelo δ=0 2.80 EXACTO; erosión / anti-proxy / sin estancamiento), 20M limpios (~5,5 h, ~1.010 fps-agente), guardias jamás disparadas; 100 semillas: FINAL 2.35/0/2.34 (Δ **−0.39/−0.48**) · mejor 18,99M 2.41/0/2.42 (Δ −0.33/−0.40); diagnóstico: la política MUEVE los drones con intención (+17% rapidez, +26% sustos — vence la habituación), sin reparto entre frentes; siguiente movimiento (aceptar v1 / --resume / co-evolución / YOLO) = decisión del usuario. → **v2.7: SUSTO DE DOS RADIOS ✓ RE-CONGELADO tag `v2.7-baseline`** (la expulsión por movimiento de v2.4 INTACTA + PARED BLANDA estática `STATIC_DETER_RADIUS`=10: un dron ACTIVE quieto a corta ya no se cruza —`_wolf_walled`, empuje radial que frena/desvía— y no deja matar a través; poste a distancia ignorable —habituación—, mover ESTRICTAMENTE mejor; SOLO `_apply_deterrence`, gateado por `escort_enabled` → face_check bit a bit; `test_pared_estatica` dirigido nuevo. RE-MEDIDO metro DGX: Dummy 4.42/0/4.34→**3.97/0/3.89**, Reactive 2.74/0/2.82→**2.30/0/2.42** (la barrera se hace pared; margen Reactive−Dummy intacto −1.67/−1.47). **INVALIDA la baseline del MARL de drones run01** —histórico sobre v2.6—; el MARL se reentrenará sobre v2.7, nota a batir **2.30/0/2.42**. NO reentrenado aquí; nº de drones 4+4 intacto —barrido 3+3 = prompt posterior—; siguiente = decisión del usuario). → **run06: CURRÍCULO del cebo (lobos, v2.7) ✓ COMPLETADO — EL CEBO NO CRUZA EL VALLE NI CON EL SPAWN SERVIDO** (currículo de separación de spawn 4 niveles ×5M = 20M: 180°+masa≥2 / 135° / 90° / normal; override SOLO del env de entrenamiento `WolfPackEnv.set_curriculum` con RNG propio → la eval SIEMPRE spawn normal; residual + recompensa de EQUIPO PURA, shaping off, sin pista de cebo; Paso 0 verificado —suelo residual δ=0 SANO, no zombi—; fase 1 clavada en el suelo eval ligera 2.20; commit del código `1ebd1fb` ANTES del run). **Trayectoria por nivel (eval ligera): L1 ~2.52 → L2 ~2.5 → L3 ~2.30 → L4 ~2.12** (degradación monótona al estrechar hacia el spawn real). **100 semillas: FINAL (20M) 1.90/1.88 → Δ −0.40/−0.54 (bajo el suelo 2.30/2.42 = erosión) · mejor 4M 2.18/2.35 (≈ suelo) · 6.5M ≈ suelo; `cebo_diag` en TODOS: cebo puro 0.0% / killer-no-detectado 0.0% = la nula del scriptado** (ni una muerte desde el frente no visto, ni a 4M con el cebo casi servido). Veredicto: el cebo NO se forma; el currículo de separación no cruza el valle (relative overgeneralization) y al endurecer erosiona. Confirma run05. **Vías de reserva (BIBLIOGRAFIA, NO implementadas — decisión del usuario): curiosidad coordinada (EMC/MACE/SMMAE/JIM) → control jerárquico de formación.** → **DIAGNÓSTICO del mundo (solo-lectura, post-run06) ✓: el cebo era GEOMÉTRICAMENTE IMPOSIBLE por percepción-oráculo de la barrera** (reaccionaba a contactos a `r_detect`=100 sabiendo el tipo por verdad-terreno → sin frente ciego; 100% de las muertes con el matador a ≤40 m de un dron —media 19-20 m—; contrafactual solo-confirmados medido: sev 2.50 vs 2.54; el mundo solo confirma UNA vez por episodio; ~50% de muertes "visto pero no disuadido" = hueco del standoff 20) → **v2.8: BARRERA HONESTA ✓ RE-CONGELADO tag `v2.8-baseline`** (la barrera reacciona SOLO a lobos CONFIRMADOS de equipo con memoria —latch a ≤`r_confirm`=40 de un ACTIVE, `_confirmed_wolves`; contacto nunca confirmado NO forma barrera— + STANDOFF DERIVADO sqrt(R²−(s/2)²)=12 m —sin hueco entre barrera y rebaño fuera de disuasión—; SOLO `coordinators.py`, world.py ≡ v2.7 → Dummy BIT A BIT 3.97/0/3.89 sin deriva; Reactive 2.30/0/2.42 → **2.56±1.55/0/2.26±1.32** —la honestidad encarece +0.26 solo-lobos, el standoff pegado abarata −0.16 mixto; margen vs Dummy −1.41/−1.63—; test 10 REHECHO (A/B dirigidos: frente ciego real) + test 11 standoff; verja 7/7; **nota a batir del MARL → 2.56/0/2.26**; el cebo deja de ser imposible por artefacto — siguiente = decisión del usuario). → **run07: currículo del cebo sobre la BARRERA HONESTA (v2.8) ✓ CERRADO — EL CEBO NO EMERGE NI CON FRENTE CIEGO REAL** (mismo currículo de run06 —4 niveles ×5M, eval SIEMPRE spawn normal— sobre v2.8; métrica CORREGIDA en cebo_diag: killer-NO-CONFIRMADO leído de la memoria de confirmación de la barrera; Paso 0: suelo SANO —sev 3.50 en 2 frentes, amurallado 15.3%, 0 timeouts—, suelo ligera v2.8 = 2.70, **nula del scriptado = 0.0% killer-no-confirmado** —el ~47.6% de la premisa era "no disuadido" a 20 m, otra métrica; la oportunidad existe: un-solo-grupo-CONFIRMADO el 36.4% de ESCOLTA—; commit `124476e` antes del run). **Desenlace: fase 1 clavada en 2.70 exacto ×3; niveles 1-3 oscilando EN o BAJO el suelo (nunca por encima; picos aislados 2.80/2.70 ≈ suelo); GUARDIA DE EROSIÓN DISPARADA a 14.75M** (6 ligeras consecutivas <2.4: 1.90/1.80/1.80/1.70/2.00/2.00); el stop no surtió efecto (LECCIÓN operativa: los runs desacoplados con `docker exec -d bash -c "nohup … &"` heredan SIGINT IGNORADO — parar con SIGTERM; en memoria) y el run completó los 20M: el nivel 4 (spawn normal) quedó clavado en 1.6-2.3 = la erosión confirmada con registro extra. **100 semillas: FINAL (20M) 1.93±1.13/1.87±1.03 (Δ −0.63/−0.39, bajo el suelo) · mejor (7.5M) 2.25/2.21 (Δ −0.31/−0.05 ≈ suelo); cebo_diag en TODOS los checkpoints (5M/7.5M/10M/20M): killer-NO-CONFIRMADO 0.0% y cebo-confirmado 0.0%** — ni una muerte de un lobo invisible para la barrera honesta, ni con el cebo servido a 180°. **Veredicto: con el artefacto de percepción ELIMINADO (v2.8) y el agujero real disponible, el cebo sigue sin formarse — el cuello es la EXPLORACIÓN (valle de relative overgeneralization: desviación coordinada temporalmente extendida que la exploración gaussiana por-paso no muestrea), NO la geometría del mundo. Tercera confirmación (run05/run06/run07); el currículo de separación de spawn queda agotado como vía. Vías de reserva (BIBLIOGRAFIA, NO implementadas — decisión del usuario): curiosidad coordinada (EMC/MACE/SMMAE/JIM) → control jerárquico de formación → co-evolución con el MARL de drones sobre "confirmado pero no disuadido" (el hueco que SÍ existe: ~50% de muertes sin dron a ≤20 m).** → **v2.9: BARRERA QUE AVANZA + CEBO DISEÑADO ✓ RE-CONGELADO tag `v2.9-baseline` — LA PRUEBA DE EXISTENCIA DEL CEBO SALE NULA EN AGREGADO** (arreglo A: la línea avanza hacia el ancla `adv=clip(L−20, 12, √(r_confirm²−16²)≈36.7)` — espantar lejos sin cegar la retaguardia; arreglo B: presa POR SECTOR — el 2º fija la MÁS LIBRE `pack_prey2`, el 1º la común; timing NO forzado; con 1 grupo bit a bit v2.8 → face 12/12). Tests: 12 (avance 36.7/repliegue 17.4 por fórmula) y 13 (el 2º sector MATA su presa libre: 5 capturas — el mecanismo ejecuta) + presa-por-sector en wolf_controller_check. RE-MEDIDO (100/tipo, 2ª pasada sin deriva): Dummy 3.98/0/3.88 (±0.01 vs v2.8) · Reactive 2.56/0/2.26 → **2.46±1.36/0/2.44±1.37** (−0.10/+0.18). **ABLACIÓN (cebo SIN avance, línea fija v2.8): 2.58/0/2.36 → efecto PURO del cebo +0.02/+0.10 ≈ RUIDO; avance −0.12/+0.08 ≈ ruido.** Lectura honesta: el cebo diseñado EJECUTA (micro) pero NO rinde en agregado contra esta defensa — al partir el paquete cada sector pierde masa/quórum y la barrera re-planificada a 15 m/s cubre frentes secuenciales casi gratis; la etapa 2 (RL aprendiendo el timing) tendría poco premio TAL CUAL. Nota a batir del MARL → **2.46/0/2.44**. Siguiente = decisión del usuario. → **v3.0: EL CEBO PERFECTO ✓ RE-CONGELADO tag `v3.0-baseline` — VEREDICTO DEFINITIVO: EL CEBO NO RINDE NI PERFECTO (la defensa arreglada lo aplasta).** Seis piezas juntas (decisión del usuario: el número NO es atribuible a cada una; se buscaba el sí/no): 1) TERRENO 500×500 solo en CONFIG_V2 (cow_spawn=(150,350) → colchón ≥100 m; lobo naciente 0/120 confirmable o detectado; defaults World 300 → face 12/12 BIT A BIT; max_steps derivado 23570) · 2) reparto de masa FIJO (`wolf_decoy_size=1` capado a n−2, asalto con cuórum; medido 1-vs-2: **anclaje IDÉNTICO 0.28 con 1 que con 2** —la masa no compra anclaje—, con 1 el asalto mata el doble → default 1) · 3) TIMING programado (`_decoy_prowl` + latch `wolf_decoy_released`; merodea sin congelarse, dispara EXACTO con el asalto a 150 m — test dirigido) · 4) BUG: la barrera PRESIONA (adv=clip(L,12,36.7) + EMBESTIDA local 30 m; test 12 dinámico: dist dron→cebo 36→19, expulsado en 69 pasos) · 5) BUG: relevo SIN PARÁLISIS (anunciado comandable hasta hand-off a blanco móvil; test battery) · 6) render: emojis a color restaurados (fuente Noto en la imagen + fix CBDT strike-fijo en `_sprite`). RE-MEDIDO (100/tipo, 2ª pasada sin deriva): Dummy 3.98/0/3.88 → **3.85/0/3.69** · Reactive 2.46/0/2.44 → **1.00±1.66/0/1.14±1.81** (success 53%/50%, n_safe 5.79/5.58) — la severidad se HUNDE: presión+relevo+margen de confirmación dominan al cebo perfecto; micro intacto (test 13). GIFs versionados en /data/gifs/v3.0/ (2 frentes cebo + 1 frente). Nota a batir del MARL → 1.00/0/1.14 (histórico). Cuarta confirmación del cebo… con un asterisco que v3.1 destapa. → **v3.1: BARRERA EN CONJUNTO + TIMING BLINDADO + COSMÉTICA ✓ RE-CONGELADO tag `v3.1-baseline` — EL EXPERIMENTO POR FIN JUSTO: LA DEFENSA COHERENTE SUBE A 2.18/2.21 (el 1.00 de v3.0 era en parte artefacto del perseguidor).** Parte 0 DIAGNOSTICADA con datos antes de tocar: (a) terreno NO se agranda — lobos nacen a 114-329 m del dron (jamás confirmables ni detectables al nacer); la "alarma al aparecer" es el REFLEJO (el investigador a 15 m/s confirma con el lobo a 94-184 m del rebaño); (b) el timing fallaba porque el cebo BAJABA de r_detect al asentar el merodeo (nace a ~114-135 del dron) y el investigador lo cazaba (0.3-2 m) confirmándolo con el asalto a 238-440 m (5/15). Arreglos: 1) la barrera avanza EN CONJUNTO — embestida por-dron ELIMINADA, adv=clip(L+STATIC(10), 12, 36.7) = sobre-apuntado de formación (test 12: colineales spacing exacto, nadie se descuelga, expulsión sin re-subidas; 2 frentes → línea ENTERA al ancla, 2º LIBRE = defensa deliberadamente explotable, mover el 2º no cambia ni un waypoint); 2) decoy_hold_dist 100→130 (r_detect+30) + huida radial PURA al invadir → 13/15 acompasados (residual 2/15 benigno: cazado al nacer pero ancla igual); 3) EMOJI_SCALE 0.27 + sin círculo de presa. RE-MEDIDO (2ª pasada sin deriva): Dummy 3.85/0/3.69 → **3.74/0/3.69** · Reactive 1.00/0/1.14 → **2.18±1.38/0/2.21±1.44** (+1.18/+1.07: la formación única con el 2º frente libre es explotable DE VERDAD). **El premio propio del cebo DENTRO de v3.1 sigue en ruido: 2-grupos 2.34/2.34 vs 1-grupo 2.11/2.15 (+0.23/+0.19 ≈ 0.9 SEM)** — la línea ocupada en cualquier frente deja huecos también sin cebo. GIFs versionados /data/gifs/v3.1/ (nº1 Dummy pasivo · nº2 barrera clásica · nº3 cebo con formación en conjunto). Nota a batir del MARL → 2.18/0/2.21. → **v3.2: BARRERA SIN HUECOS + AVANCE REAL + TIMING MEDIDO ✓ RE-CONGELADO tag `v3.2-baseline` — LA DEFENSA COMPACTA QUE PERSIGUE BAJA A 1.91/0/1.96 (el criterio de aceptación fue el COMPORTAMIENTO EN GIF, no el test).** Diagnóstico previo con datos: la línea v3.1 vivía pegada al rebaño (centro ≤37 del centroide; solo perseguía con el ancla a <27 = la banda de su test) → 17.9% de pasos con la formación a <1 m/s, 75.4% de encuentros silenciosos, ~4.6 cruces/ep por franjas sin pared; el timing "13/15 OK" de v3.1 medía el estado al asentarse, NO el instante del disparo — con la métrica correcta (dist del asalto a su presa al saltar ESCOLTA) daba 58% ≤150. Arreglos: 1) spacing 32→20 = 2·STATIC (paredes que se tocan; 0 cruces limpios; flancos rodeables = limitación aceptada); 2) persecución acotada al anillo `MAX_ADVANCE_FROM_HERD=50` del centroide de collares (45% de pasos con persecución real; centro ≤ anillo, 0 violaciones); 3) asalto pre-disparo con ARCO OSCURO + PICADO → timing 63% ≤150 mediana 143 (resto estructural: 13% cebo cazado al nacer + ~9% asalto barrido al nacer; 4 vías refutadas CON MEDIDA documentadas en wolf_controllers.py); 4) emojis 0.20. RE-MEDIDO (2ª pasada sin deriva): Dummy 3.74/0/3.69 → **3.77/0/3.74** (+0.03/+0.05 ≈ ruido, solo el arco) · Reactive 2.18/0/2.21 → **1.91±1.35/0/1.96±1.56** (−0.27/−0.25: la defensa coherente Y compacta defiende mejor; n_safe 4.90/4.95). GIFs /data/gifs/v3.2/ (nº1 asedio sin huecos sev=0 · nº2 barrera que avanza 97% persecución · nº3 timing con el asalto a 142 m) + v32_frame_emojis.png. Nota a batir del MARL en v3.2 → 1.91/0/1.96. → **v3.3 (SIN congelar, parada por regla del prompt) + v3.4: LÍNEA RÍGIDA ✓ RE-CONGELADO tag `v3.4-baseline` — EL MULTI-FRENTE POR FIN RINDE: Reactive 1.91/1.96 → 2.68/2.77.** v3.3 (tres métricas del usuario como criterio duro; 1 pasó, 2 toparon estructura → PARADA con números): M1 TRINQUETE ✓ (dist centro→vaca próxima crece 58% ventanas, series 13→50-60); M2 cierre local 4.2→1.2 cruces-hacia-dentro/ep (no llegó a ~0: la pared congelada canaliza); M3 ROLES INVERTIDOS (cebo se muestra + ala rota; asalto oscuro por anillos): ancla=cebo 13%→60% (techo ~73% por nacimientos condenados), ≤150 en 42-45% (frontera ~1:1). El usuario ACEPTÓ cebo+sellado en GIF y pidió v3.4: **barrera = CUERPO RÍGIDO** (una pose C,u + offsets fijos; ningún objetivo individual; CIERRE ELIMINADO con su coste medido 56→149 cruces/28 eps — reportado, sin parches por-dron) **+ GOBERNADOR DEL MÁS REZAGADO** (paso_pose = max(0, DRONE_MAX_SPEED·dt − err_max); fórmula documentada; formación espera QUIETA). Rigidez MEDIDA y ACOTADA (FORMADA 85%: err p95 1.66/máx 4.71 · spacing 19.7-20.3 · colin p95 0.20) y VISIBLE en GIF; M1 intacta (57.9%). Tests adaptados sin bajar listón (reactive 2/10/12 + timing del check de lobos a la semántica v3.3/v3.4); verja 7/7. RE-MEDIDO (2ª pasada sin deriva): Dummy **3.82/0/3.84** (+0.05/+0.10 ≈ el lado lobo apenas toca al Dummy) · Reactive **2.68±1.66/0/2.77±1.71** (+0.77/+0.81: el cebo invertido ancla la línea única y el asalto entra por el frente libre — sexta lectura del cebo, la primera POSITIVA). GIFs /data/gifs/v3.4/ + raíz (nº1 seed95 línea rígida 2→50 m ancla=cebo · nº2 seed76 asalto a 146 m, 6 muertes, candidato al nº3 de la narrativa). Nota a batir del MARL → **2.68/0/2.77**. Siguiente = decisión del usuario.
*(Los coordinadores van DESPUÉS de congelar v2: v2 es la referencia que deben batir. Baseline HONESTA con Dummy+guiado+disuasión PASIVA: TASA ~82%, **SEVERIDAD ~4.40 muertes/ep** (el pin la subió desde ~1.55 FALSEADA; el rodeo la bajó a ~2.73; afinar la disuasión —radio 40→20 + bordeo— la subió a ~4.17: el dron reacciona solo de cerca → el Dummy QUIETO cubre poco; la pareja madre-ternero lenta la dejó en ~4.05; que los lobos dejen de pillarse en la zona segura la subió a ~4.40 —más eficientes, ya no pierden tiempo—; máx. 8). Adversario puro ~6.33. **v2 CONGELADA — se mide POR TIPO (N=100/tipo, `baseline.py`): solo-lobos 4.45±2.15 (= corzos OFF), solo-corzos 0.00 (sin amenaza), mixto 4.41±2.18 (≈ solo-lobos)** (el agregado mezclado 2.95 es poco informativo). **v2.3 (SUSTO FUERTE) RE-MIDE la baseline: Dummy 2.36 / 0.00 / 2.24 (la disuasión fuerte casi la halva —el Dummy QUIETO ya expulsa a los lobos que se acercan a sus drones—); Reactive 0.16 / 0 / 0.18 (barrera + susto = protección casi total, 85% success).** **v2.4 (SUSTO POR MOVIMIENTO) RE-MIDE de nuevo: el estático ya no disuade (los depredadores se habitúan) → Dummy 4.41 / 0.00 / 4.34 (≈ v2.2: la disuasión estática se evapora); Reactive 2.80 / 0 / 2.78 (sube desde 0.16/0.18 —su barrera pasa tiempo quieta— pero SIGUE batiendo al Dummy −1.61/−1.56 porque se recoloca).** La SEVERIDAD es la métrica principal; la baja el coordinador **MOVIENDO** drones con intención (no basta clavarlos), y con corzos además aprende a NO malgastar drones en lo que no es amenaza.)*

## Convenciones de código
Arrays NumPy `(N,2)` · RNG sembrado (reproducible bit a bit) · SI, `dt=0.1` · geometría derivada de `min(W,H)` · **sin números mágicos** (las constantes físicas son absolutas y etiquetadas cerca de cabecera) · `render.py` es **solo reproducción** (nunca llama a `step()`; entidades como **emojis** de color —sprites PIL `embedded_color`, tamaño `EMOJI_SCALE` afinable, 🐄🐺🦌🐗🚁, fallback a marcadores si no hay fuente; SIN leyenda de entidades —se explican solos— + **🔊 bajo el dron que EMBISTE a un lobo** (v2.4: el render LEE el flag `drone_scaring` del snapshot —que el mundo calcula en `_apply_deterrence`—, no la mera cercanía: un dron QUIETO ya no "ladra")— + **barra de batería** sobre cada dron; `main.py --coordinador dummy|reactive` elige política y añade `battery` al history —render/main solo LEEN).

## Estado (commits)
`194a3ad` base · `37910b3` terminal · `e663504` disparador por dron · `4d1e708` campo 300 + escala absoluta · `886bd45` dispersión · `a15e2df` mov. drones (3a) · `fd893b8` detectar→confirmar (3b) · `49e0e22` consolidar docs · `144b7bd` paso 2 guiado · `1d44cdc` máx. 1 caza (REVERTIDO) · `56ff75d` huida no-holonómica · `f42456f` 2 fixes huida · `26bce79` disuasión del dron · `bd57a8f` matanza excedente · `1b54b49` fix pin (envolvente + disuasión parcial) · `bbee8c0` evitación al huir (las no-fijadas rodean a los lobos; sev v2 ~2.27) · `e15d43d` el más cercano investiga (reflejo elige el ACTIVE libre MÁS CERCANO; sev v2 ~2.33) · `5a9dddb` afinar disuasión (radio 40→20 + TANGENCIAL bordeo + frenazo 0.5→0.7; reacciona solo de cerca y arquea; sev v2 ~4.17) · `101558f` la madre no abandona al ternero (huye a `calf_speed`=0.8; pareja lenta; sev v2 ~4.05) · `4250488` los lobos no se pillan en la zona segura (TANGENCIAL en la frontera; bordean, no entran; sev v2 ~4.40) · `bda6156` corzos (3c): cuerpos NO-amenaza (deambulan+huyen, no cazan), detectables, ORÁCULO a r_confirm (lobo→ESCOLTA / corzo→descarta), 3 tipos de episodio ~1/3; OFF por defecto (bit a bit) · `e44d7c2` main.py spawnea corzos + `--escenario` · `9e11a29` corzos: el dron VUELA e investiga (fix) + agrupados + render natural · **congelar v2 (tag `v2-baseline`): `CONFIG_V2` (corzos ON, 3 tipos) + `baseline.py` = ARNÉS de evaluación POR TIPO; severidad CONGELADA solo-lobos 4.45±2.15 / solo-corzos 0.00 / mixto 4.41±2.18 (N=100, range(100)); `baseline_v2.json`/`.csv`; verja 12/12 verde bit a bit** (`b04e8d9`, tag `v2-baseline`) · **ReactiveCoordinator (barrera de apantallado — 1er coordinador clásico, regla fija): sev por tipo 3.27 / 0 / 3.40 vs Dummy 4.45 / 0 / 4.41 (N=100, mismo arnés); `reactive_check.py` + `reactive_eval.py` + `baseline_v2_reactive.*`; `world.py`/`baseline.py` intactos, verja verde** (`2ad268b`) · **relevo de flota REALISTA (v2.1): hand-off SIN teletransporte (estados `INCOMING`/`STRANDED`; el bajo se clava en su puesto y cubre hasta que llega el relevo; `RETURNING`→central→`CHARGING`; `STRANDED` bajo estrés); solo `_step_battery`/`_init_battery`/free-mask; RE-CONGELADO tag `v2.1-baseline`, Dummy 4.45/0/4.41 y Reactive 3.27/0/3.40 SIN cambios; `battery_check` actualizado; face_check 12/12 bit a bit** (`bcd407f`, tag `v2.1-baseline`) · **render: entidades como EMOJIS a color (sprites PIL) + barra de batería sobre cada dron + `main.py --coordinador dummy|reactive`** (`e14206a`) · **jabalí 🐗 como 2ª distracción (~50/50 con corzo `distraction_species_prob`, MISMO comportamiento, substream RNG separado → spawns intactos, descartado a r_confirm) + emojis MÁS PEQUEÑOS (`EMOJI_SCALE`); RE-CONGELADO tag `v2.2-baseline`, Dummy 4.45/0/4.41 y Reactive 3.27/0/3.40 SIN cambios; face_check 12/12 bit a bit** (`11dc90d`, tag `v2.2-baseline`) · **retoques visuales + fix arranque del reactivo: render (emojis `EMOJI_SCALE` 0.55→0.45, SIN leyenda de entidades, 🔊 bajo el dron ACTIVE que disuade —lobo a ≤`DETER_RADIUS`—) + `_patrol` ancla la fase de la formación a la posición angular ACTUAL de los drones (nacían en las esquinas ~225°+90°i y se mandaban a la ranura ~135° OPUESTA → cruzaban el centro; ahora a su ranura MÁS CERCANA: error 135°→10°, sep 16→41 m, sin cruces); Reactive 3.27/3.40→**3.36/3.42** (+0.09/+0.02, el bug apoyaba números en center-hugging), Dummy/física INTACTOS (NO re-congela); `test_arranque` nuevo + `test_severidad_muestra` n=15→30 (n=15 caía en el slice de menor beneficio y el ruido cambiaba el signo); face 12/12 bit a bit** (`a97362d`) · **SUSTO FUERTE + rombo de carga (v2.3, tag `v2.3-baseline`): la disuasión pasa de PARCIAL a fuerte —un dron ACTIVE a ≤`DETER_RADIUS` EXPULSA al lobo (huida RADIAL creciente al acercarse, acotada [`SCARE_SPEED_MIN`=0.8, wolf_speed]; la huida SUSTITUYE a la caza) y este NO mata mientras huye (`_wolf_scared` excluido de `_process_predation`); SIN excepción a corta (fuera `deter_w`/`DETER_REPULSION`/`TANGENT`/`SLOWDOWN`)— arregla el 'cuadro congelado' lobo-vaca-dron; slots de reserva en ROMBO. RE-MEDIDO: Dummy 4.45/4.41→**2.36/2.24**, Reactive 3.36/3.42→**0.16/0.18** (barrera+susto ≈ protección total, 85% success). `test_susto` nuevo + tests de disuasión adaptados al susto (k_con=None, EXPULSIÓN) + fix del bug del `sev()` de reactive_check (world congelado). face 12/12 bit a bit; verja verde** (`49fd8c4`, tag `v2.3-baseline`) · **SUSTO POR MOVIMIENTO + baterías espejo + carga 1.5× (v2.4, tag `v2.4-baseline`): el lobo se HABITÚA a disuasores estáticos → solo un dron con velocidad de APROXIMACIÓN `> SCARE_APPROACH_MIN`=1.0 hacia el lobo (a ≤`DETER_RADIUS`) lo EXPULSA (mecanismo v2.3); un dron ESTÁTICO/alejándose a ≤`SCARE_STATIC_RADIUS`=6 es solo un OBSTÁCULO (esquiva suave, el lobo SIGUE cazando, puede matar con un dron parado al lado). Flag `_drone_scaring` → 🔊 solo al embestir. Baterías iniciales ALEATORIAS `[battery_init_min=0.25,1]` + reserva ESPEJO (`1−pareja`) del substream `_battery_rng` (seed+2_000_003, spawns bit a bit). Carga = `CHARGE_TO_FLIGHT_RATIO`=1.5× el vuelo pleno → `charge_full` DERIVADO ≈160 s (param DEPRECATED). RE-MEDIDO: la disuasión estática se evapora → Dummy 2.36/2.24→**4.41/4.34** (≈v2.2), Reactive 0.16/0.18→**2.80/2.78** (sube —barrera clavada = poste— pero SIGUE batiendo al Dummy −1.61/−1.56). `test_susto`/`test_disuasion` adaptados al modelo por movimiento (QUIETO=obstáculo, EMBISTE=expulsión), `test_pin_envolvente` (un poste NO protege → k_con≠None), battery_check +espejo +ratio (estrés 2.5→5.0× por la carga más rápida). face 12/12 bit a bit (fingerprint de combate idéntico v2.3≡v2.4); verja verde** (`7cf7381`, tag `v2.4-baseline`) · **REFACTOR: controlador de lobos ENCHUFABLE (`wolf_controllers.py`): `WolfController.decide(world) → (v_target, coasting)`; `ScriptedWolfController` (default) = v2.4 BIT A BIT; `_update_wolves` = orquestador (decide → susto → inercia/integración); `pack_prey` sigue en el World (contrato compartido con el pin de la vaca); salida = velocidad; `World(wolf_policy=)` + `main.py --lobos`; `learned` = hueco RL. Fingerprint de equivalencia IDÉNTICO (`4425c866…`, combate + escolta Dummy + Reactive); verja verde SIN adaptar; `wolf_controller_check.py` nuevo (susto innegociable + spot-check baseline). SIN re-congelar (v2.4-baseline vigente)** (`aa77e0a`) · **ANDAMIAJE RL de LOBOS: contenedor + envoltorio Gymnasium + train_wolves + smoke. `docker/` (imagen calcada del lab, pins exactos + libgl1; `${USER}-wolves` idle, GPU count:1, repo→/workspace, ~/rl_data→/data, `conectar.sh`) · `rl/` (`RLWolfController`: acción del env con CAP por norma EN LA FRONTERA —world.py congelado NO se toca—, `pack_prey` regla fija ternero-primero, coasting determinista; `WolfPackEnv` Gymnasium: obs fija (122) padding+máscaras, acción Box(-1,1,(10,)) cada 5 pasos, recompensa RALA +1/muerte, lobos/mixto ~50/50 con semilla fresca por reset, Reactive congelado dentro; `train_wolves.py` PPO SB3 + SubprocVecEnv(fork) → artefactos a /data) · `baseline.py` extendido RETROCOMPATIBLE (`wolf_controller[_factory]=None` = bit a bit) · `rl_env_check.py` NUEVO en la verja (6 tests: layout/determinismo/cap/presa/CANAL DE RECOMPENSA dirigido/física intacta). rl_env_check 6/6 + smoke 60k (681 fps, sin NaNs, artefactos persistentes en ~/rl_data) verdes dentro del contenedor; `learned` sigue NotImplementedError. **HALLAZGO + DECISIÓN: la baseline congelada era DEPENDIENTE DEL ENTORNO** — en la DGX (proyecto Y lab) Dummy daba 4.54/0/4.46 vs los 4.41/0/4.34 del portátil (deriva FP entre plataformas amplificada por caos; código exonerado: HEAD da lo mismo dentro). Decisión (usuario): **el contenedor de la DGX pasa a ser el METRO CANÓNICO** → re-medición completa dentro (`baseline.py` + `reactive_eval.py`, sin tunear nada), `REFERENCE_SEVERITY`+`FROZEN_TAG`+artefactos actualizados, asserts de tag de wolf_controller_check/rl_env_check → v2.4.1. **RE-CONGELADO tag `v2.4.1-baseline` ("mismo mundo, metro DGX"): Dummy 4.54/0/4.46 · Reactive 2.77/0/2.80 (nuevo objetivo del MARL)**; verja 7/7 verde dentro del contenedor; el portátil deja de ser referencia (sus spot-checks pueden salir rojos — esperado)** (`6b936c4`, tag `v2.4.1-baseline`) · **EVALUADOR de lobos aprendidos + 1er run serio: `rl/obs.py` (obs de ORIGEN ÚNICO: env y evaluador comparten `build_obs`) · `rl/policy_wolf_controller.py` (`PolicyWolfController` autónomo —modelo SB3 dentro, mismo frame-skip, hereda cap/presa/coasting— + `SyncedReactiveCoordinator`: el refresco de la política va en `act()`, la FRONTERA del step, el mismo instante en que el env muestrea; equivalencia env↔evaluador BIT A BIT = rl_env_check test 7, 2.829 fronteras idénticas) · `rl/eval_wolves.py` (arnés de siempre, mismas 100 semillas, vs scriptados 2.77/2.80; JSON a /data) · train: gamma 0.999, `--resume`, `train.log`, eval ligera cada 250k, ckpt cada 500k (`e1e1b31`) · run01 (10M, 12 envs, CPU, desacoplado) ABORTADO a 2,48M por el CRITERIO PACTADO: ep_rew_mean 0.000 sostenido (UNA muerte espontánea aislada a ~1,08M sin efecto; 10/10 evals deterministas 0.00; fps ~1.070; ep_len ~1-1,5k = el rebaño se refugia). La recompensa RALA no arranca contra la barrera; el shaping (plan B) NO implementado — DECISIÓN DEL USUARIO pendiente. Checkpoints en ~/rl_data/wolves/run01/** (`e1e1b31` código + `9fc906f` docs del desenlace) · **PLAN B ACTIVADO (decisión del usuario): SHAPING POR POTENCIAL + run02 de cero. `WolfPackEnv(shaping=, shaping_beta=, shaping_gamma=)`: r = r_kills + (γΦ′−Φ), Φ = −β·dist media a la presa ternero-primero (GUARDAR/RESTAURAR: el pin no ve el cálculo)/diagonal, 0 en coasting; γ = el del PPO (0.999) pasado al env; default env OFF ≡ run01 bit a bit, `--shaping` de train def. ON; componentes en info y en train.log (ep_kills_mean = LA señal, ep_shape_mean, kills en summary); eval SIEMPRE sin shaping. `rl_env_check` test 8 NUEVO (telescopia Σγ^t·r_shape == γ^T·Φ_T−Φ_0 exacta y acotada 2β / signo ±/ kills == Δn_depredadas / off≡run01 obs bit a bit). Smoke 60k shaping ON: ep_shape_mean 0.78–0.95 ≠ 0 desde el 1er rollout, kills 0 (esperado). CRITERIO DE ABORTO run02 (cabecera del log): DECIDE ep_kills_mean ≈ 0.00 a ~3M → parar (plan C = usuario). run02 lanzado de cero tras este commit (10M, shaping ON, β=1, /data/wolves/run02)** (`c77686d`) · **run02 COMPLETADO (10M, ~3 h, criterio NO saltó: kills buffer 0→0.35-0.40, evals ligeras pico 0.80; eval 100 semillas: FINAL 0.57±0.72/0.60±0.75 = ABLACIÓN desde-cero) + PLAN C construido (decisión del usuario: clonar al scriptado y afinar): `rl/collect_demos.py` (fronteras del env, semillas 10k+ disjuntas, presa del CONTRATO impuesta, 120.214 pares/150 eps, experto 2.59) + `rl/bc_pretrain.py` (misma π [128,128], solo política, loss enmascarada mse|dir, bc_model.zip SB3) + `train_wolves --init-from/--lr/--abort-ref` (copia solo π, value fresco, aborto warm-start). CUNA ≈0 → INVESTIGACIÓN: pipeline exonerado (scriptado mismo camino 2.7/2.7/2.5), causa = INFRAAJUSTE de [128,128] sobre el envolvente (|pred| 0.62 vs 1.000, 17% cosenos negativos, train≈val; en cerrado cruza la barrera, merodea a r_face_safe, no flanquea) → run03 NO LANZADO (puerta pactada cuna ≥1.5); opciones A red mayor/B DAgger/C run03 igualmente/D +shaping SIN implementar. Verja 8/8+face+wolf_controller verde** (`2620471`; docs `0d1e5fb`) · **opción A (usuario): π [256,256] (NET_ARCH; ablación run02 = cross-arquitectura) → clon SIGUE 0.0/10 (val dir 0.424→0.406: no era capacidad) → HALLAZGO: ALIASING de la etiqueta (31,4% de 244.681 parejas consecutivas con cos<0 a 0,5 s; bimodal estable-o-flip) — la etiqueta de primer-paso es cuasi-ruido en ~⅓ del dataset; run03 sigue SIN lanzarse (rama ≈0); candidatos A′ (etiqueta=media de la ventana) / C (run03 desde el clon) / B (DAgger)** (`d7ecba4`) · **opción A′ (usuario): `collect_demos --label mean|first` (v3 = media de la ventana, mismos episodios) + TECHO medido en vivo (oráculo deepcopy((mundo,coordinador)) + hold vía RLWolfController, script en /data/wolves/diag/) = 2.60 → etiqueta EXONERADA; BC [256,256] sobre v3 = 0.2/10 → rama ≈0: run03 NO lanzado; estratificación: VIAJE cos 0.83 (por eso llega) / MANIOBRA cos 0.26, 37% opuesta (por eso no mata) → el cuello es la RED sobre la obs congelada; recomendación C (run03 desde el clon v3). Verja verde** (`3d99c13`) · **DECISIÓN (C): run03 desde el clon v3 CON shaping (β=1; cuna sin recompensa densa + invariancia + run02-vs-run03 aísla la cuna), lr 1e-4, 10M, `--abort-ref 0.2` (a ~3M sin superar con claridad ~0.2 → parar; nota adaptada en ABORT_NOTE_WARMSTART); lanzado desacoplado tras el commit** (`058dbfd`) · **DESENLACE run03: 10M completos (~4,6 h; criterio NO saltó — ligera 0.10→0.70 a 3,25M→picos 0.80; buffer kills 0.45-0.48 y a 900k ya al nivel FINAL de run02). Tablas 100 semillas: FINAL 0.65±0.68/0.72±0.85 · mejor (ckpt 4M) 0.66±0.79/0.71±0.85 — robusto al checkpoint; vs las 4 referencias (scriptado 2.77/2.80 · cuna 0.2 · run02 0.57/0.60 · techo 2.60): LA CUNA APORTA (+0.08/+0.12, único cambio el init), el scriptado sigue lejos (la maniobra de flanqueo es el muro). Artefactos en ~/rl_data/wolves/run03/; siguiente movimiento = usuario (más presupuesto sobre run03 / Φ de flanqueo con cuidado anti-proxy / DAgger / aceptar 0.65-0.72 como lobos v1 y pasar al MARL)** (`ed16a81`) · **run04 RESIDUAL (RPL): `rl/residual_wolf_controller.py` (script vivo dentro + δ; obs 132 con pista del script; modo eval con SyncedReactiveCoordinator; model=None ⇒ δ≡0 = SUELO) + env `residual=True` + train 2 fases (init a cero verificado, fase 1 solo-crítico, guardia del suelo 2.3) + `eval_wolves --residual/--floor` + test 9 (δ=0 ≡ scriptado bit a bit 1.023 pasos) + BIBLIOGRAFIA.md (RPL/RRC/JSRL/KL/Ng/PPO/SB3/Gymnasium + §12 de DISEÑO consolidada); smoke residual: init 0 exacto, dos fases, kills 2.3-2.6 desde el 1er rollout; run04 lanzado desacoplado tras el commit** (`74b3bab`) · **DESENLACE run04: 10M limpios (~4,3 h, ~650 fps); fase 1 = 4 evals ligeras CLAVADAS en 2.90 con detalle idéntico (termómetro del suelo); fase 2 rango 2.60-3.20, guardia (2.3) jamás amenazada. 100 semillas: MEJOR (1,5M) 2.82±1.49/2.81±1.43 → Δ +0.05/+0.01 (≈ suelo 2.78/2.81, dentro del ruido) · FINAL (10M) 2.61±1.36/2.66±1.44 → Δ −0.16/−0.14 (erosión leve). Δ ≈ +0.0: PPO con autoridad plena no halló adaptación anti-dron que generalice → el scriptado ya está ≈ en el techo explotable contra esta barrera; FASE DE LOBOS APRENDIDOS CERRADA (4 runs medidos), el scriptado = lobo canónico** (`7f36144`) · **v2.5: SPAWN DE LOBOS EN SUBGRUPOS (Nivel A, RE-CONGELADO tag `v2.5-baseline`): `wolf_spawn_mode="clustered"|"grouped"` (default World clustered ≡ v2.4.1 BIT A BIT — face/battery/escort/drone intactos; CONFIG_V2 y main.py piden grouped); `_split_wolves_groups` con substream `_wolf_group_rng` (seed+3_000_003): n≤2 → 1 grupo; n>2 → 1 ó 2 al 50/50; 1 grupo ≡ clustered EXACTO; 2 grupos → k~unif{1..n−1} últimos índices (cebo k=1 posible), 2º sector ≥60°, ancla al MISMO borde. Diagnóstico 4/4 (determinismo/substream/geometría/distribución 58/114≈50%) + `v25_spawn_grouped.gif` + fidelidad CONFIG_V2 ≡ defaults+corzos+grouped. RE-MEDIDO (100/tipo, contenedor): hipótesis multi-frente NO confirmada con el scriptado — Dummy 4.54/0/4.46→**4.42/0/4.34**, Reactive 2.77/0/2.80→**2.71/0/2.76** (~1 SEM de bajada: fija UNA presa común, el 2º grupo no ceba; el cebo = Nivel B/RL). Artefactos regenerados (2ª pasada reproducida exacta), REFERENCE_SEVERITY/FROZEN_TAG/asserts de tag → v2.5. El MARL pasa a batir **2.71/0/2.76**. PARADO tras congelar (sin re-correr la campaña de lobos)** (`9401b78`, tag `v2.5-baseline`) · **v2.6: BARRERA REACTIVA CON PERCEPCIÓN REALISTA (RE-CONGELADO tag `v2.6-baseline`): la barrera se anclaba a `wolves.mean()` = OMNISCIENCIA (lobos que ningún dron había visto entraban en la media; el reconocimiento previo mostró que con 2 frentes el eje seguía a esa media). Ahora SOLO ve lobos DETECTADOS (≤ `r_detect` de un dron ACTIVE — el criterio del disparador del mundo, recomputado en solo-lectura en `coordinators.py`; world.py INTACTO) y el eje se ancla al PRIMER detectado con HISTÉRESIS (al perderse → el siguiente más antiguo; desempate índice menor); PENETRADO/cobertura solo sobre detectados; en ESCOLTA sin detectados → PATRULLA. `test_percepcion` NUEVO (nº 10 de reactive_check: mover el frente NO visto deja los waypoints IDÉNTICOS; eje |vs ancla|=3.7° vs |vs media|=61.5°; sin detectados → anillo; re-detección → barrera); tests 1-9 SIN cambios de aserción; verja 7/7 verde en el contenedor. RE-MEDIDO (100/tipo, 2ª pasada bit a bit): Dummy **4.42/0/4.34** BIT A BIT ≡ v2.5 (no usa la barrera) · Reactive **2.74±1.47/0/2.82±1.49** (v2.5: 2.71/0/2.76 → +0.03/+0.06, dentro del ruido — contra el scriptado convergente la media global ≈ percepción local; lo que cambia DE VERDAD: el frente no visto ya no influye → el CEBO del Nivel B es físicamente real). Artefactos con frozen_tag v2.6; REFERENCE_SEVERITY sin cambio. El MARL pasa a batir **2.74/0/2.82**** (`19a97ab`, tag `v2.6-baseline`) · **run05 NIVEL B (lobos residuales que aprendan el CEBO, recompensa de equipo PURA): mismo andamiaje que run04 sobre el mundo v2.6 grouped (ResidualWolfController sin cambios; δ autoridad plena = wolf_speed; 2 fases; shaping OFF confirmado en config —sin término de cebo, sin pista por-lobo—); guardias run05 en ABORT_NOTE_RESIDUAL (suelo 2.4 sostenido ≥1M · estancamiento a ~4M sin superar ~2.8 → parar; run04 usó 2.3 sin estancamiento — histórico); `rl/cebo_diag.py` NUEVO (episodios de 2 subgrupos del arnés: fracción de muertes del grupo NO anclado / sujeción del frente visto / CEBO PURO killer-no-detectado∧otro-visto; nula del scriptado: 0.0% cebo puro, 46.1% no-anclado post-fusión, 25.6% de pasos con un solo grupo visto); suelo v2.6 grouped verificado 2.74/2.83 (+0.00/+0.01, /data/wolves/run05_floor_v26.json); eval_wolves referencias → artefacto vigente; smoke 2 fases OK (init 0 exacto, kills desde el 1er tramo); verja 7/7 verde; run05 lanzado desacoplado tras el commit (10M, 12 envs, lr 1e-4, /data/wolves/run05_nivelB)** (`8f44d0e`) · **DESENLACE run05: guardia de ESTANCAMIENTO saltó a 4.2M (12 ligeras de fase 2 en 2.50-3.10 ≈ suelo 2.90, sin tendencia; fase 1 clavada 2.90 ×4 idénticas; ~614 fps; kill -INT limpio, checkpoints 0.5M-4M). 100 semillas: 4M 2.67±1.47/2.71±1.49 (Δ −0.07/−0.11) · 3.5M 2.80±1.60/2.71±1.55 (Δ +0.06/−0.11) vs suelo 2.74/2.83. cebo_diag: **0.0% killer-no-detectado / 0.0% cebo puro en ambos = la nula del scriptado** (ni una muerte desde el frente no visto; no-anclado 40.4-40.8% vs 46.1% nula; sev 2-grupos 3.33→3.09/3.24). El cebo NO emerge con recompensa de equipo pura: es una desviación coordinada temporalmente extendida con valle de recompensa en medio — la exploración por-paso no la muestrea. Siguiente movimiento = decisión del usuario (pista al cebo / formación explícita / co-evolución / MARL contra 2.74/0/2.82)** (`f5c0d8a`) · **INFRA MARL de DRONES (MAPPO/CTDE residual sobre la barrera; SIN run serio): `rl/drone_obs.py` (obs por PUESTO: local 131 —ego+commandable+pista del waypoint base, lobos SOLO detectados, sin batería/corzos— ‖ global privilegiada 122 = build_obs reutilizada) · `rl/residual_drone_coordinator.py` (barrera v2.6 viva dentro; δ al waypoint con máscara load-bearing ACTIVE&~inv&~hold; model=None ⇒ suelo) · `rl/drone_env.py` (DroneTeamEnv 3 kinds + recompensa global −1/muerte + local de disuasión `deter_credit` POR SEPARADO —anti-proxy—; TeamUnstackVecEnv M mundos→4M streams) · `rl/train_drones.py` (MAPPO sobre SB3 pinneado: SplitMlpExtractor π←local/V←global; receta residual: init δ≡0 assert + log_std −2 + 2 fases; LightEval por severidad, MENOS=mejor) · `rl/drone_eval.py` (misma vara; --floor). VERIFICADO en contenedor: SUELO 100 semillas δ=0 → 2.74/0.00/2.82 Δ +0.00 EXACTO en los 3 tipos; rl_env_check test 10 (layout/percepción · suelo bit a bit episodio completo · máscara —δ enorme no desvía RETURNING/investigador— · canal global+local exacto + atribución dirigida); verja 7/7; smoke 2 fases ~880 fps-agente con ligera de fase 1 clavada en el suelo. BIBLIOGRAFIA: +DE-MADDPG (Sheikh & Bölöni, arXiv:2003.10598, escolta defensiva con recompensa individual+equipo) +escort arXiv:1910.04537 +COMA/difference-rewards/VDN/QMIX (descartadas por sobre-ingeniería). Sin dependencias nuevas; §3.2 (acciones discretas) superada por δ continua (anotado en DISEÑO)** (`aac480d`) · **run01 MARL de drones: PACTO de guardias fijado ANTES del run (guardias INVERTIDAS: EROSIÓN ligera sostenida >~2.9 en fase 2 ≥1,5M · ANTI-PROXY ep_deter↑ sin ep_sev↓ ≥2M con el par impreso en CADA eval · SIN aborto por estancamiento) + eval ligera del pacto 10 semillas FIJAS 5 lobos+5 mixto (suelo δ=0 medido 2.80 EXACTO); verja 7/7; run01 20M lanzado tras el commit** (`c215689`) · **docs: DESENLACE run01 MARL — EL MARL BATE LA BARRERA: 20M limpios (~5,5 h, ~1.010 fps-agente), guardias jamás disparadas (fase 1 clavada 2.80×4 con detalle exacto; cero evals >2.9; cero patrón proxy — deter oscilando MIENTRAS la severidad bajaba); 100 semillas: FINAL (20M) 2.35±1.33/0/2.34±1.42 = Δ −0.39/−0.48 vs barrera (~3 SEM; n_safe 4.54/4.54; vs Dummy −2.07/−2.00) · mejor ckpt 18,99M 2.41/0/2.42 (Δ −0.33/−0.40) — robusto al checkpoint; diagnóstico de comportamiento (58 gemelos de 2 subgrupos, /data/drones/diag/run01_comportamiento.py): el Δ = MOVERSE PARA DISUADIR (+17% rapidez ACTIVE 3.76→4.39 m/s, +26% lobos huyendo/paso 1.19→1.50, sev 2-frentes 3.31→2.90) y NO reparto entre frentes (6.2%→6.5% idéntico) — la política vence la habituación v2.4, el agujero de la línea única sin explotar (sin presión selectiva: el scriptado no ceba); la mejora crecía al agotar el presupuesto (evals >18M media 2.125) → --resume candidato; siguiente movimiento = decisión del usuario** (`01a591d`) · **v2.7 SUSTO DE DOS RADIOS (tag `v2.7-baseline`): pared blanda estática + re-medición (Dummy 3.97/0/3.89, Reactive 2.30/0/2.42)** (`d103ef3`) · **run06 currículo del cebo: código+verificación antes del run** (`1ebd1fb`) · **DESENLACE run06: el cebo no cruza el valle ni con el spawn servido (cebo_diag 0.0% en todos los ckpts; final erosiona a 1.90/1.88)** (`de8d578`) · **v2.8 BARRERA HONESTA (tag `v2.8-baseline`): la barrera reacciona SOLO a lobos CONFIRMADOS de equipo con memoria (latch a ≤`r_confirm`=40 de un ACTIVE, `_confirmed_wolves` en coordinators.py; fin de la percepción-oráculo: un contacto nunca confirmado NO forma barrera aunque esté a ≤`r_detect`) + STANDOFF DERIVADO sqrt(R²−(s/2)²)=12 m (sin hueco entre barrera y rebaño fuera de disuasión); world.py INTACTO ≡ v2.7 → Dummy BIT A BIT 3.97/0/3.89 (sin deriva); Reactive re-medido 2.30/0/2.42 → 2.56±1.55/0/2.26±1.32 (la honestidad encarece +0.26 solo-lobos, el standoff pegado abarata −0.16 mixto; margen vs Dummy −1.41/−1.63); reactive_check test 10 REHECHO (A: contacto a 57 m no dispara barrera→patrulla / cruza 40→confirmado→barrera / se aleja a 249 m→memoria la rastrea; B: frente sin confirmar movido→waypoints idénticos = FRENTE CIEGO real) + test 11 standoff (peor hueco 20.0 ≤ DETER_RADIUS) + umbral test_reactivo 5→3 recalibrado (escala con el standoff); verja 7/7 (con FIX colateral destapado: el residual de lobos con δ≡0 renormalizaba `v_script` — no era no-op cuando el script deja la norma 1 ulp sobre `wolf_speed` → divergencia ~1e-16 amplificada por el caos; ahora PASS-THROUGH exacto con δ≡0, cap intacto con δ≠0; el residual de drones ya era puro); INVALIDA la nota a batir del MARL (2.30/2.42 era vs barrera-oráculo) → nota nueva 2.56/0/2.26; el frente ciego hace el cebo geométricamente POSIBLE por fin** (`437c131`) · **run07 código: currículo sobre v2.8 + métrica de cebo CORREGIDA (killer-no-confirmado de la memoria de la barrera) + Paso 0 (suelo sano 2.70; nula corregida 0.0%)** (`124476e`) · **DESENLACE run07: EL CEBO NO EMERGE NI CON FRENTE CIEGO REAL — guardia de erosión disparada a 14.75M (stop fallido por SIGINT ignorado → lección SIGTERM en memoria; el run completó 20M y el nivel 4 confirmó la erosión); 100 semillas FINAL 1.93/1.87 (Δ −0.63/−0.39) · mejor 7.5M 2.25/2.21 (≈suelo); cebo_diag 0.0% killer-no-confirmado en TODOS los checkpoints; tercera confirmación (run05/06/07) → el cuello es la EXPLORACIÓN, no la geometría; currículo de spawn agotado como vía** (`064408f`) · **v2.9 BARRERA QUE AVANZA + CEBO DISEÑADO (tag `v2.9-baseline`): A) coordinators.py — la línea avanza al ancla, adv=clip(L−DETER, 12, √(r_confirm²−(s/2)²)≈36.7) (retaguardia confirmable; repliegue al mínimo v2.8 al apretar); B) world.py+wolf_controllers.py — presa POR SECTOR en grouped-2 (`pack_prey2` = la MÁS LIBRE: máx dist al ACTIVE más cercano, ternero-primero, capacidad del sector, anti-convergencia; pin de ambas presas; capturas con is_pack_prey2; táctica por sector con la misma maquinaria; camino de 1 grupo INTACTO bit a bit — face 12/12); tests 12+13 + presa-por-sector; verja 7/7; RE-MEDIDO: Dummy 3.98/0/3.88 (±0.01) · Reactive 2.46/0/2.44 (vs v2.8 2.56/2.26 = −0.10/+0.18); ABLACIÓN cebo-sin-avance 2.58/0/2.36 → cebo PURO +0.02/+0.10 ≈ RUIDO = LA PRUEBA DE EXISTENCIA SALE NULA en agregado (micro ejecuta: 5 capturas del 2º sector); nota a batir del MARL → 2.46/0/2.44** (`cd20ccb`, tag `v2.9-baseline`) · **v3.0 EL CEBO PERFECTO (tag `v3.0-baseline`): terreno 500 (CONFIG_V2, colchón ≥100, defaults 300 intactos) + reparto fijo decoy=1 (medido 1-vs-2: anclaje idéntico 0.28) + timing programado (merodeo `_decoy_prowl` + `wolf_decoy_released`) + FIX barrera que PRESIONA (adv=clip(L,12,36.7) + embestida 30 m) + FIX relevo sin parálisis (anunciado comandable, hand-off a blanco móvil, STRANDED congelado in situ) + FIX render emojis (fuente en imagen + strike CBDT); tests: 12 PRESIÓN re-hecho + 13 CEBO PERFECTO + test_timing_cebo + test_relevo_sin_paralisis; verja 7/7; RE-MEDIDO: Dummy 3.85/0/3.69 · Reactive **1.00/0/1.14** → VEREDICTO: el cebo NO rinde ni perfecto — los fixes de la defensa lo aplastan (success 53%/50%); GIFs versionados /data/gifs/v3.0/; nota a batir del MARL → 1.00/0/1.14** (`b857be1`, tag `v3.0-baseline`) · **v3.1 BARRERA EN CONJUNTO + TIMING BLINDADO (tag `v3.1-baseline`): Parte 0 diagnosticada con datos (terreno exonerado; el investigador cazaba al cebo que caía dentro de r_detect); formación entera al ancla (embestida por-dron eliminada; adv=clip(L+10, 12, 36.7); 2º frente confirmado LIBRE por diseño — test 12 rehecho: colineal/spacing/2-frentes/dinámico), decoy_hold 130 + huida pura (13/15 acompasados), emojis 0.27, sin círculo de presa (fix lista de artistas del render); verja 7/7; RE-MEDIDO: Dummy 3.74/0/3.69 · Reactive **2.18/0/2.21** (+1.18/+1.07 vs v3.0: el 1.00 era artefacto del perseguidor; premio propio del cebo +0.23/+0.19 ≈ ruido); GIFs /data/gifs/v3.1/; nota a batir del MARL → 2.18/0/2.21** (`5897e68`, tag `v3.1-baseline`) · **v3.2 BARRERA SIN HUECOS + AVANCE REAL + TIMING MEDIDO (tag `v3.2-baseline`): el criterio de aceptación = COMPORTAMIENTO EN GIF (los tests de v3.1 daban verde con la barrera quieta, agujereada y el timing mal medido). Diagnósticos con datos ANTES de tocar (v32_parte0/v32_race en /data/wolves/diag/): línea pegada al rebaño (17.9% pasos <1 m/s; 75.4% encuentros silenciosos; 4.6 cruces/ep por franjas sin pared) y carrera del timing anatomizada (inv@==det@ 78/78; confirma ~15 m tras detectar → pasa ⟺ detectado a ≤165 de la presa). Arreglos: spacing 32→20=2·STATIC (paredes que se tocan, 0 cruces limpios; standoff derivado 17.3; flancos rodeables aceptados) + persecución del ancla acotada al anillo MAX_ADVANCE_FROM_HERD=50 del centroide de collares (45% pasos con persecución real; sustituye advance_max; espalda del anillo = agujero deliberado) + ARCO OSCURO + PICADO del asalto pre-disparo (63% ≤150 mediana 143 vs 58%/144; 4 vías refutadas con medida documentadas en wolf_controllers.py; cola estructural: 13% cebo cazado al nacer, ~9% asalto barrido al nacer) + emojis 0.20; tests 11/12 rehechos (paredes que tocan + fórmula del anillo + persecución dinámica con centro ≤ anillo); verja 7/7; RE-MEDIDO (2ª pasada sin deriva): Dummy **3.77/0/3.74** (+0.03/+0.05 ≈ ruido) · Reactive **1.91/0/1.96** (−0.27/−0.25 vs v3.1: compacta+perseguidora defiende mejor); GIFs /data/gifs/v3.2/ + copias en la raíz; nota a batir del MARL → 1.91/0/1.96** (`3bf39a5`, tag `v3.2-baseline`) · **v3.4 LÍNEA RÍGIDA + ROLES INVERTIDOS DEL CEBO (tag `v3.4-baseline`; incluye la v3.3 aceptada por el usuario EN GIF tras su parada reglamentaria): A) v3.3-M1 TRINQUETE (coordinators.py) — aim monótono hacia fuera por episodio (métrica del usuario: 58% ventanas, series 13→50-60 sostenidas); B) v3.3-M3 ROLES INVERTIDOS (wolf_controllers.py) — el cebo se deja confirmar PRIMERO (show + ALA ROTA arrastrando al investigador) y el asalto espera OSCURO por anillos STAGE/CREEP/DEEP hasta estacionarse (gate `_assault_staged` = pegado al sobre ∧ ≤trigger+LEAD 60); ancla=cebo 13%→60% (techo ~73% por nacimientos condenados: 27% del spawn), ≤150 al saltar ESCOLTA 42-45%, frontera ancla↔profundidad ~1:1 (8 configs; vías refutadas con medida en wolf_controllers.py); C) v3.4 LÍNEA RÍGIDA (regla del usuario: si un dron avanza, avanzan TODOS) — UNA pose (C,u) + offsets FIJOS (waypoint_i = C+off_i·perp SIEMPRE; CIERRE LOCAL v3.3 ELIMINADO por deformación, coste MEDIDO: cruces 56→149/28 eps, hacia-dentro-en-pie 33→92, PENETRADO 4159→13593 — reportado sin parches por-dron) + GOBERNADOR DEL MÁS REZAGADO (paso_pose=max(0, DRONE_MAX_SPEED·dt−err_max), cota triangular |Δc|+|Δφ|·r_max; idempotente por paso; en la (re)formación la pose nace en el objetivo y espera QUIETA); RIGIDEZ MEDIDA (FORMADA=85% de CLEAN): err estación p95 1.66/máx 4.71 · spacing 19.7-20.3 · colinealidad p95 0.20 · M1 intacta 57.9%; agujeros DELIBERADOS: flancos + cruces residuales + espalda del anillo + 2º frente (explotado); tests adaptados sin bajar listón (reactive 2 reposición EN EL TIEMPO umbral 3 m · 10 clones deepcopy solo-frente-ciego + convergencia 200 · 12 REHECHO cuerpo rígido exacto + gobernador acotado no-vacuo · timing del check de lobos al gate v3.3 + show ≤ r_detect); verja 7/7; RE-MEDIDO (2ª pasada sin deriva): Dummy **3.82/0/3.84** (+0.05/+0.10) · Reactive **2.68±1.66/0/2.77±1.71** (+0.77/+0.81 vs v3.2 — EL MULTI-FRENTE POR FIN RINDE: el cebo invertido ancla la línea única y el asalto entra por el frente libre; sexta lectura del cebo, la primera positiva); GIFs /data/gifs/v3.4/ + copias en la raíz (nº1 seed95 línea rígida 2→50 ancla=cebo · nº2 seed76 asalto a 146, 6 muertes); REFERENCE_SEVERITY/FROZEN_TAG/asserts → v3.4; nota a batir del MARL → 2.68/0/2.77** (este commit).
